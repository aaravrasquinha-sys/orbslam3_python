# Phase 7 (Build 1) Runbook — Data-Model Invariants + Map Coverage

Companion to the earlier phase runbooks. Read §1 and §4.1 before
debugging anything related to observation counts, dangling references,
or map-point culling — this build found the third and fourth instances
of a bug SHAPE Phase 6 already found once, and built a permanent tool
(`validate.py`) specifically so a fifth instance doesn't need to be
found by hand again.

Also read this if you're picking up Build 2 (the real-time iSAM2 back
end) — everything here is what it will be built on top of.

---

## 0. Context change before this build started

The project's operating requirement changed mid-stream from "produce the
most accurate possible offline reconstruction" to "run live on a
slow-moving robot in the facility." That reframe is documented in full
in `PHASE7_ARCHITECTURE.md` (the original batch-oriented plan) and
`PHASE7_ARCHITECTURE_V2_REALTIME.md` (the revision — read this one
first; it supersedes the back-end design in the first doc but the
front-end/data-model findings in both are unaffected by the reframe).
This build (Build 1) covers Workstreams A and B from those documents.
Build 2 (real-time iSAM2 back end) and Build 3 (IMU + evaluation)
follow in later phases and are NOT in this zip.

Per an explicit instruction for this phase: **Phase 4 (dense RGB-D
fallback) is removed entirely**, not just deprioritized. `dense_
odometry.py` and `test_phase4_gate.py` are gone; every reference to
them elsewhere is cleaned up. If you're diffing against `slam_v2_
phase0_6.zip`, don't be alarmed that these files are missing — that's
deliberate, see PROGRESS.md's Phase 7 section for the reasoning.

---

## 1. What changed, in one paragraph

Three data-model invariant bugs were found and fixed at the root:
`MapPoint.observations` was being polluted with non-keyframe frame ids
(a Phase 6 fix that over-corrected), `kf_seq` was a per-map counter that
silently invalidated point ages across a merge, and RGB-D mode never
triangulated far points at all, leaving the map with zero far-field
structure to hold onto during rotation. A new module, `validate.py`,
checks eight Atlas-wide invariants and is meant to be run constantly,
not just when something looks wrong. Running it for the first time
surfaced FOUR more bugs this phase wasn't looking for, three of them the
same underlying failure shape (a dict-based observation registry going
stale relative to what a keyframe's own array actually holds) that
Phase 6 already found once — this is now the third and fourth time this
exact shape has recurred, in different call sites. Map coverage also
improved: stratified spatial point selection, rotation-triggered
keyframe insertion, and the `emitter_on` manifest column (written since
early on, never consumed) is now actually used.

---

## 2. Architecture map

```
┌─────────────────────────────────────────────────────────────────┐
│  UNCHANGED FROM PHASE 1-6                                          │
│    Everything in the earlier runbooks still applies                 │
├─────────────────────────────────────────────────────────────────┤
│  NEW THIS PHASE                                                     │
│    validate.py         — 8 Atlas-wide invariant checks +            │
│                          reference_validity_rate() (promotes the     │
│                          old FIELD_LOG_PHASE6.md Sec.2 hand-check)   │
│    Matcher.dedupe_by_train_idx() — matcher.py, new helper            │
├─────────────────────────────────────────────────────────────────┤
│  FIXED AT THE ROOT (Workstream A)                                   │
│    frame.py / map.py     — kf_seq now global (Frame._next_kf_seq),  │
│                            assigned once, never renumbered           │
│    merge_maps.py         — no longer renumbers kf_seq on merge      │
│    tracking.py (x2), relocalization.py — removed the Phase-6         │
│                            add_observation() calls that polluted    │
│                            .observations with non-keyframe ids       │
├─────────────────────────────────────────────────────────────────┤
│  FOUND VIA validate.py, NOT WHAT THIS PHASE SET OUT TO FIX           │
│    local_mapping.py — _triangulate_new_points had no uniqueness     │
│                       guard on the NEIGHBOR side (real gap, fixed,   │
│                       but not the main cause -- see below)          │
│    matcher.py         — match_ratio()/knnMatch() has no train-index  │
│                       uniqueness guarantee; THE main root cause      │
│                       (existed since Phase 3, never triggered a      │
│                       failure until this phase's own front-end       │
│                       changes made it fire often enough)             │
│    fusion.py           — _fuse_point never cleared the ABSORBED       │
│                       point's own .observations after redirecting    │
│                       its entries -- the fourth instance of this     │
│                       failure shape                                  │
│    map.py              — erase_map_point/clean_bad_points got a      │
│                       defensive ownership check as a second,         │
│                       independent layer against the same shape       │
├─────────────────────────────────────────────────────────────────┤
│  CHANGED (Workstream B, map coverage)                                │
│    local_mapping.py — close/far split (th_depth), far-point          │
│                       triangulation in RGB-D mode, stratified        │
│                       spatial point selection                        │
│    tracking.py        — rotation-triggered keyframe insertion        │
│    run_dataset.py     — consumes emitter_on (was written, unread)    │
│    config.py           — depth_max, max_new_points_per_kf,           │
│                       keyframe_min_displacement, th_depth,           │
│                       keyframe_min_rotation_deg all changed/added    │
├─────────────────────────────────────────────────────────────────┤
│  REMOVED ENTIRELY                                                     │
│    dense_odometry.py, test_phase4_gate.py, synthetic.py's            │
│    textureless_segment_sequence() -- Phase 4 dropped per explicit    │
│    instruction                                                        │
├─────────────────────────────────────────────────────────────────┤
│  STILL NOT DONE                                                       │
│    iSAM2 real-time back end (Build 2), IMU wiring + evaluation       │
│    harness (Build 3), dense reconstruction (Phase 9)                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. Pre-flight checklist

**3.1 — Run `validate.py` on any Atlas state you have, first, before
anything else.** This is the single most useful health check this
build introduced, and it now runs in code rather than by hand:
```python
import validate
report = validate.validate_atlas(slam.atlas)
print(report)   # empty error/warning lists = clean
n_valid, n_total, pct = validate.reference_validity_rate(slam.atlas)
print(f"{n_valid}/{n_total} = {pct:.1f}%")
```
Expect **zero errors and >99% reference validity** on any healthy run —
this project's own synthetic measurement after all Build 1 fixes landed
was 100.0% on both a plain scroll sequence and a rotation-heavy yaw
sequence. If you see errors, the printed messages name the exact
keyframe/point ids involved — that's enough to start tracing without
needing to reproduce the ablation methodology from scratch (see
PROGRESS.md's Phase 7 section for the full trace of how each of the
four newly-found bugs was isolated, as a worked example).

**3.2 — Run the full seven-phase gate before hardware.**
```bash
python3 test_phase7_gate.py
```
This internally re-runs Phases 1, 2, 3, 5, and 6's own gates (Phase 4's
gate no longer exists — see §0) and exercises all of Build 1's own
fixes directly. Budget several minutes; it processes multiple synthetic
sequences through the real `SLAMSystem` and shells out to run every
earlier phase's gate script as a subprocess.

**3.3 — Confirm the vocabulary cache exists** before running anything
that touches loop closing or relocalization (unchanged requirement from
Phase 5/6, restated because a clean checkout of this zip won't have it):
```bash
ls orbvoc_cache.npz || python3 setup_vocabulary.py
```

**3.4 — Confirm which BA backend is actually active.** Unchanged from
Phase 3, still worth checking since Build 2 will change this
significantly:
```bash
python3 -c "from config import DEFAULTS as c; print(c['bundle_adjust']['backend'])"
```

---

## 4. Symptom → diagnosis → fix

### 4.1 — `validate.py` reports errors, or reference validity drops below 99%

Read the SPECIFIC error messages first — they name the exact keyframe id,
point id, and (where relevant) keypoint index involved, which is enough
to start tracing without guessing. Before assuming this is a brand-new
bug, check whether it matches one of the four failure shapes this phase
already found and fixed (all documented in detail in PROGRESS.md's Phase
7 section, with the exact tracing technique used for each):

- **`check_observations_are_keyframes` fires** ("references frame X,
  which is not a live keyframe") — look for a NEW `add_observation()`
  call site that might be running on non-keyframe frames. Grep for it:
  ```bash
  grep -n "add_observation" *.py
  ```
  Every call site should be either (a) constructing a brand-new
  MapPoint with an empty observations dict, or (b) inside `local_
  mapping.process_new_keyframe`'s reconciliation step, or (c) inside
  `fusion.py`'s two sites (which now correctly use the stale-index
  return value — see below). If you've added a new one that registers
  an ordinary TRACKED (non-keyframe) frame, that's the Phase 6 bug
  recurring in a new location.

- **`check_map_point_id_consistency` fires** ("dangling reference") —
  this is the symptom, not the cause. Trace the SPECIFIC point id's full
  lifecycle the same way PROGRESS.md's Phase 7 section does: hook
  `MapPoint.__init__`, `MapPoint.add_observation`, `Map.erase_map_point`,
  and `Map.clean_bad_points` to print whenever they touch that exact id,
  then re-run the failing scenario. The four bugs this phase found were
  all discovered exactly this way — don't guess, trace.

- **A caller re-registers an already-observing frame at a NEW keypoint
  index** — this is the general failure shape underlying THREE of the
  four bugs found this phase (see PROGRESS.md's bugs 5 and 6). `MapPoint.
  add_observation()` now RETURNS the previous keypoint index if this
  happens (`None` if it's a genuinely new observation). **Any caller
  that might legitimately re-register an existing frame_id at a
  different index MUST use this return value** to clear the OLD
  keyframe-side `map_point_ids[old_idx]` slot — see `fusion.py`'s two
  call sites for the pattern. If you add a new caller that could hit
  this (anything involving point fusion, merging, or re-matching an
  already-tracked point) and don't use the return value, you will
  silently reintroduce this bug family.

- **A one-directional matcher call produces a duplicate correspondence**
  — `Matcher.match()` and `Matcher.match_ratio()` (which wraps `cv2`'s
  `knnMatch`) both guarantee a unique QUERY index per call but make NO
  such guarantee on the TRAIN index. If you add a new call site that
  builds `frame.map_point_ids[m.queryIdx] = some_id` directly from
  matcher output, call `Matcher.dedupe_by_train_idx(matches)` on the
  result FIRST (matches are already sorted best-distance-first, so this
  keeps the best match per train index and drops worse duplicates). This
  was the actual root cause of the largest reference-validity failure
  found this phase (93.5%, should be >99%) — see PROGRESS.md bug 5 for
  the full trace, including why it existed since Phase 3 without ever
  being caught before this phase's own validator existed.

### 4.2 — Map coverage or point counts look different from Phase 1-6 baselines

Expected, and not itself a problem. Several deliberate changes affect
this: `max_new_points_per_kf` 100→300, `depth_max` 3.5m→6.0m,
`keyframe_min_displacement` 0.10m→0.25m (fewer, more spread-out
keyframes), and RGB-D mode now triangulates far points where it
previously created zero. If you have EXTERNAL baselines recorded before
this phase, don't expect them to match exactly — check `validate.py`
and `test_phase7_gate.py` instead, which were written/recalibrated
against the current behavior.

### 4.3 — A synthetic scene with a single fixed depth doesn't exercise the close/far split

Expected — `scroll_sequence()` and `yaw_sequence()` both use ONE depth
for the whole scene, so every point is either all-close or all-far and
the split can't be meaningfully tested with them. Use `synthetic.
two_depth_scroll_sequence()` instead (new this phase) — it composites a
near plane and a far plane in the same frame specifically for this.

### 4.4 — Rotation-triggered keyframes seem to fire too often or too rarely

`keyframe_min_rotation_deg` (default 15°) is a straightforward
threshold against `imu.log_so3`'s angle between the current frame's pose
and the last keyframe's — there's no smoothing or hysteresis. If real
hardware shows this firing on ordinary head-turn jitter, raise it; if it
never fires during genuine rotation, lower it and re-check with
`test_phase7_gate.py`'s Test 9 pattern (a direct unit test against
`Tracking.needs_new_keyframe`, no full pipeline needed) before touching
anything else.

### 4.5 — `run_dataset.py --ir` recordings behave differently than before

Expected — tracking now uses ONLY emitter-off (clean) frames and borrows
depth from the nearest emitter-on neighbor, roughly halving the
EFFECTIVE tracked frame rate for `--ir` recordings specifically (plain
RGB recordings, with no `emitter_on` manifest column, are completely
unaffected). This was previously recorded but silently ignored; the
emitter's dot pattern was going straight into the extractor on every
frame. If this regresses tracking quality on a specific recording,
check first whether that recording actually alternates the emitter (has
a real `emitter_on` column with both True/False values) — a recording
made without `--ir` won't trigger this code path at all.

---

## 5. Testing order

1. `validate.py` on a fresh, short recording (§3.1) — confirms the core
  fixes are in place before testing anything more elaborate.
2. Full `test_phase7_gate.py` suite (§3.2).
3. `two_depth_scroll_sequence()` (§4.3) if you're specifically verifying
  the close/far split rather than trusting the gate suite's own check.
4. A real facility recording, `run_dataset.py`, checking `validate.py`'s
  reference-validity percentage stays >99% throughout — not just at the
  end, since the four bugs found this phase all manifested as gradual
  degradation, not an immediate crash.

## 6. What "success" looks like for this build

1. `validate.py` reads zero errors and >99% reference validity on real
  hardware footage, not just synthetic tests.
2. A rotation-heavy real segment (turning to look down a corridor, the
  original failure mode from the very first forensic pass on this
  project) shows tracked point count holding up better than Phase 1-6
  baselines, because far points now survive rotation where they
  previously didn't exist at all.
3. No unexplained new fragmentation compared to Phase 5/6 baselines —
  the config changes (larger keyframe spacing, more points per keyframe)
  should if anything reduce map-lifecycle churn, not increase it.
4. If you recorded with `--ir`, `run_dataset.py`'s output shows the
  `[emitter]` log line reporting a real on/off frame split, not silence
  (silence means the manifest doesn't have the column at all — check
  the recording was actually made with `--ir`).

# Field Log — Phase 7, Build 1 Hardware Verification (Data-Model Invariants + Map Coverage)

Same rules as before: actual command output, not a prose summary. Build
1 has NOT been tested on real hardware at all yet. Write "N/A" rather
than skip a field silently. Phase 4 (dense RGB-D fallback) is REMOVED
this phase — don't run any `--dense` or `use_dense_fallback` commands
from earlier field logs, they no longer exist.

---

## 0. Environment

```bash
python3 -c "import cv2, gtsam; print('opencv', cv2.__version__, 'gtsam ok')"
ls orbvoc_cache.npz || echo "MISSING -- run setup_vocabulary.py first"
python3 -c "from config import DEFAULTS as c; print(c['local_mapping']); print(c['tracking'])"
grep -c "add_observation" tracking.py fusion.py relocalization.py local_mapping.py
```
**Paste output:**
```
[paste here]
```

**Confirm no dense-fallback leftovers:**
```bash
grep -rn "dense_odometry\|use_dense_fallback" *.py
```
**Expect zero matches. Paste output:**
```
[paste here]
```

---

## 1. Synthetic gates (no camera needed)

```bash
python3 test_phase7_gate.py 2>&1 | tail -60
```
**Paste the full tail output, and how long the whole thing took:**
```
[paste here]
```

Time taken: **___ seconds**

---

## 2. validate.py health check (the most important new diagnostic this build)

Run this on ANY map state you can get access to during/after a real test:
```python
import validate
report = validate.validate_atlas(slam.atlas)
print(report)
n_valid, n_total, pct = validate.reference_validity_rate(slam.atlas)
print(f"{n_valid}/{n_total} = {pct:.1f}%")
```
**Paste output:**
```
[paste here]
```
**Errors present?** [ ] Yes  [ ] No — if yes, paste every error message
verbatim (they name specific keyframe/point ids, needed for tracing):
```
[paste here]
```
**Reference validity percentage:** ______%  (expect >99%; this
project's synthetic measurement after all Build 1 fixes was 100.0% on
both a plain scroll and a rotation-heavy yaw sequence)

---

## 3. Close/far point split on a real scene

Point the camera at a scene with BOTH a near surface (<2m) and a
farther one (>2m) visible simultaneously — a hallway looking down its
length works well.

```python
near_pts, far_pts = 0, 0
for mp in slam.atlas.active_map.map_points.values():
    if mp.is_bad: continue
    z = mp.position[2]  # or appropriate depth axis for your world frame
    if z < 2.0: near_pts += 1
    else: far_pts += 1
print(f"near={near_pts} far={far_pts}")
```
**Paste output:**
```
[paste here]
```
**Both near and far points present?** [ ] Yes  [ ] No

---

## 4. Rotation-triggered keyframe insertion

Do a slow, deliberate ~30-45 degree yaw with minimal translation (turn
in place, or pivot at a corner) and check the keyframe log:

Command run:
```bash
python3 run_dataset.py --dataset [your dataset] 2>&1 | grep -A2 "\[TRACK\]" | tail -60
```
**Paste a stretch covering the rotation:**
```
[paste here]
```
**Did a keyframe get created during the rotation despite minimal
translation?** [ ] Yes  [ ] No

---

## 5. `--ir` emitter-alternation handling (skip this section entirely if you didn't record with `--ir`)

```bash
python3 run_dataset.py --dataset [your --ir dataset]
```
**Paste the `[emitter]` log line near the start of output:**
```
[paste here]
```
**Tracked frame count roughly half the total recorded frames?**
[ ] Yes  [ ] No  [ ] N/A (not an --ir recording)

---

## 6. The 30cm-forward/turn/30cm-back loop test

Command run:
```bash
python3 run_dataset.py --dataset [your dataset] --sequence-short-loop
```
(or however this test is invoked in your setup — match the pattern from
earlier field logs)

**Final summary block:**
```
[paste here]
```

**map_health() dict:**
```python
import metrics
print(metrics.map_health(slam))
```
```
[paste here]
```

**validate.py check on this same run (§2 again, different scenario):**
```
[paste here]
```

---

## 7. Comparison note

If you have a Phase 5 or 6 field log from the SAME physical test setup
(same room, same motion), how does this run's point count / far-point
presence / keyframe count compare? (doesn't need to be precise, just a
sanity comparison — expect MORE keyframes than before given the larger
`keyframe_min_displacement`, but SPREAD OUT more, and expect far points
to exist where before there would be none at all)
```
[paste here]
```

---

## 8. Self-check before sending

* [ ] `orbvoc_cache.npz` exists (or was generated via `setup_vocabulary.py`)
* [ ] No `dense_odometry`/`use_dense_fallback` references anywhere (§0)
* [ ] `validate.py` shows zero errors and >99% reference validity on
     every run in this log, not just the synthetic gate
* [ ] Far points confirmed present on a real scene with depth variation (§3)
* [ ] Rotation-triggered keyframe confirmed on a real deliberate turn (§4)

---

## 9. Anything else noticed

```
[paste here]
```


Complete Phase 7 (Build 1) File Changes Summary

TL;DR: 25 files modified, 6 new, 2 deleted.

25 MODIFIED FILES

Workstream A fixes (data-model invariants):

frame.py — global Frame._next_kf_seq counter added
map.py — add_keyframe uses global kf_seq, not per-map len(self.keyframes)
merge_maps.py — removed kf_seq renumbering loop (now just sorts)

Observation-pollution fixes (Phase 6's over-correction + three new bugs found via validate.py):
4. tracking.py — REMOVED add_observation() from _track_local_map and _solve_pnp
5. relocalization.py — REMOVED add_observation() from try_relocalize
6. map_point.py — add_observation() now returns stale keypoint_idx if frame re-registered at different index
7. local_mapping.py — defensive stale-index handling in process_new_keyframe
8. fusion.py — _fuse_point uses stale-index return value, clears absorb.observations
9. fusion.py — _project_and_fuse uses same stale-index pattern

Workstream B (map coverage):
10. local_mapping.py — close/far split at th_depth, stratified selection, far-point triangulation
11. tracking.py — rotation-triggered keyframe insertion (needs_new_keyframe c4 check)
12. config.py — 6 parameter changes: depth_max, th_depth, max_new_points_per_kf, keyframe_min_displacement, keyframe_min_rotation_deg, and removed use_dense_fallback/dense_depth_trunc

Dense fallback removal:
13. frame.py — removed keep_dense parameter
14. run_slam.py — removed keep_dense=True, removed _pending_dense_cleanup
15. run_dataset.py — removed keep_dense parameter

Emitter handling (newly consumed):
16. run_dataset.py — added emitter_on column logic (track only off-frames, borrow depth from on-frames)

Matcher deduplication (trainIdx bug fix):
17. matcher.py — added dedupe_by_train_idx() helper
18. tracking.py — call dedupe_by_train_idx() in _solve_pnp
19. relocalization.py — call dedupe_by_train_idx() in try_relocalize
20. initializer.py — call dedupe_by_train_idx() in init_monocular

Test suite:
21. test_phase5_gate.py — removed Phase 4 from regression list
22. test_phase6_gate.py — removed use_dense_fallback line

Synthetic sequences:
23. synthetic.py — added two_depth_scroll_sequence(), removed textureless_segment_sequence()

Documentation:
24. PROGRESS.md — added full Phase 7 section (6 bugs, 4 found by validate.py, complete trace methodology)
25. requirements.txt — added gtsam, documented numpy<2.0 constraint

6 NEW FILES
validate.py — 8 Atlas-wide invariant checks
test_phase7_gate.py — 11-test suite (9 new + 2 regressions)
RUNBOOK_PHASE7.md — hardware troubleshooting guide
FIELD_LOG_PHASE7.md — hardware testing template
PHASE7_ARCHITECTURE.md — original batch-oriented design doc
PHASE7_ARCHITECTURE_V2_REALTIME.md — revised real-time design (supersedes batch for Build 2+)
2 DELETED FILES
dense_odometry.py — Phase 4 module
test_phase4_gate.py — Phase 4 gate

All files with change details are in /mnt/user-data/outputs/PHASE7_FILE_CHANGES.txt for easy reference.





# Phase 8 Runbook — IMU-Based Motion Prediction for Tracking Robustness

Companion to the earlier phase runbooks. Read §1 before touching
anything IMU-related — this phase's central lesson is that code which
*looks* complete (a real prediction strategy wired into `track()`, a
real staged-initialization module, real preintegration) can still be
completely broken if nothing has ever actually run it end-to-end.

---

## 0. Context: this phase did not start from zero, and did not find zero bugs either

`tracking.py`'s IMU-prediction strategy, `imu.py`'s preintegration, and
`imu_init.py`'s staged initialization all predate this phase — verified
by diffing against the original uploaded `slam_v2_phase0_6.zip`, not
assumed. What this phase actually did was discover that none of it had
ever been exercised end-to-end, and fix the four real bugs that were
consequently sitting there undetected. See `PROGRESS.md`'s Phase 8
section for the full account of each. In one line each:

1. `run_slam.py`'s `_try_imu_init` had no `def` line — dead code, would
  crash on first use with `use_imu=True`.
2. `run_realsense()` never enabled the D435i's motion streams or passed
  `use_imu=True` — live hardware had no path to feed IMU data in at all.
3. `frame.velocity` only got set inside one specific success path in
  `track()` — any other path succeeding first would silently and
  permanently disable IMU prediction for the rest of the run.
4. `local_inertial_bundle_adjust` hung indefinitely on a dense finite-
  difference Jacobian — the exact same failure mode this project's
  original BA-hang bug had, just never fixed for this sibling function.

All four are fixed and gated (`test_phase8_gate.py`). **None of this has
been tested on real hardware yet** — everything so far is synthetic,
noise-free, perfectly-synchronized ground truth. Real BMI085 data will
be noisier and won't be perfectly synchronized with the camera by
construction — see §3 and `FIELD_LOG_PHASE8.md`.

---

## 1. Architecture map

```
┌─────────────────────────────────────────────────────────────────┐
│  PRE-EXISTED THIS PHASE (verified correct via new ground truth)     │
│    imu.py's Preintegration recursion                                 │
│    imu_init.py's staged (gyro bias -> gravity/bias/velocity) solve  │
│    tracking.py's _predict_pose_imu (the math itself was fine)        │
├─────────────────────────────────────────────────────────────────┤
│  FIXED THIS PHASE                                                    │
│    run_slam.py    — _try_imu_init given back its missing `def` line  │
│    run_slam.py    — run_realsense(): motion streams, --imu flag,     │
│                     --t_cam_imu flag, live buffer + sync/slice loop  │
│    tracking.py    — velocity propagates on EVERY successful track    │
│                     once IMU is initialized, not just Strategy 0's   │
│                     own success branch                                │
│    bundle_adjust.py — local_inertial_bundle_adjust given a           │
│                     jac_sparsity pattern (was: none at all)          │
├─────────────────────────────────────────────────────────────────┤
│  NEW THIS PHASE                                                       │
│    synthetic.py — imu_ground_truth_sequence() (closed-form analytic  │
│                  trajectory + matching IMU samples)                  │
│    synthetic.py — imu_visual_sequence() (matching camera frames,     │
│                  same underlying trajectory)                         │
│    test_phase8_gate.py — 7 tests: 2 pure-math ground-truth checks,   │
│                  3 direct bug regressions, 1 full-pipeline           │
│                  integration check, 1 full Phase 1-7 regression      │
├─────────────────────────────────────────────────────────────────┤
│  STILL NOT DONE                                                       │
│    Real hardware verification (this phase is synthetic-only so far)  │
│    Per-keyframe bias re-estimation (imu_init.py's documented scope   │
│    limitation -- bias is estimated once, carried forward as a        │
│    constant, not re-optimized as a random walk)                      │
│    VIBA1/VIBA2-style re-refinement passes at ~5s/~15s (also          │
│    documented, also deferred)                                        │
│    Real-time iSAM2 back end (Phase 9) -- IMU factors joining that    │
│    graph directly is future work beyond this phase                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. Pre-flight checklist

**2.1 — Confirm the vocabulary cache exists** (unchanged requirement,
restated because a clean checkout won't have it):
```bash
ls orbvoc_cache.npz || python3 setup_vocabulary.py
```

**2.2 — Confirm `_try_imu_init` is real** before trusting anything else:
```bash
python3 -c "from run_slam import SLAMSystem; print(hasattr(SLAMSystem, '_try_imu_init'))"
```
Must print `True`. If it prints `False`, something has reintroduced Bug
1 — check for a missing `def` line the same way it was found originally
(a docstring/body with no signature above it, usually from a bad
find-and-replace or merge).

**2.3 — Run the full gate before hardware.**
```bash
python3 test_phase8_gate.py
```
This is slow (several minutes) — it runs a real synthetic visual-
inertial pipeline through `SLAMSystem`, then re-runs Phases 1, 2, 3, 5,
6, and 7's own gates as subprocesses (Phase 7's gate itself re-runs 1,
2, 3, 5, 6 again internally — the nesting is real, budget accordingly,
10-15 minutes is normal).

**2.4 — Never run `--imu` on `run_realsense()` without also having
recorded a real `T_cam_imu.txt`** (via `record.py --imu`, which saves
the factory extrinsic) if prediction accuracy matters for what you're
testing. `--t_cam_imu` defaults to identity rotation if omitted — fine
for confirming nothing crashes, not fine for judging whether prediction
is actually helping.

---

## 3. Symptom → diagnosis → fix

### 3.1 — `use_imu=True` crashes immediately with `AttributeError: '_try_imu_init'`

This is Bug 1 exactly. Check §2.2. If it's genuinely missing again,
look for a `def _try_imu_init(self, world_map):` line directly above
the docstring that starts `"""Attempt staged inertial initialization...`
in `run_slam.py` — if that signature is gone, the whole block silently
becomes dead code hanging off the previous method.

### 3.2 — `run_realsense() --imu` doesn't seem to have any effect

Check that `args.imu` is actually reaching `SLAMSystem(..., use_imu=args.imu)` and that motion streams are enabled BEFORE `pipeline.start(config)` — both must happen, and both are in `run_realsense()` now (§1). If you're on `--frames` or plain `--realsense` without `--imu`, this is expected: there is no IMU source in either of those paths.

### 3.3 — IMU never initializes on real hardware (`imu_initialized` stays False)

Check the `[imu-init] not ready yet: ...` log line for the specific
reason (`imu_init.py`'s `observability_gate` gives a human-readable
cause). The two most likely on real hardware:
- **Insufficient rotational excitation** — the operator needs to
  deliberately rotate the camera during the first couple of seconds,
  not just walk in a straight line. `min_gyro_std` in `config.py`'s
  `imu` section controls the threshold.
- **Insufficient accelerational excitation** — some genuine stop-start
  motion (not perfectly constant velocity) is needed in the first
  couple of seconds too. `min_accel_std` controls this threshold.

This is a real physical limitation of the two-stage linear approach
(documented in `imu_init.py`'s own docstring), not a bug — see that
module's `observability_gate` docstring for why gravity/accel-bias are
fundamentally unobservable without this kind of excitation.

### 3.4 — IMU initializes but then gives visibly bad predictions

Most likely causes, roughly in order of likelihood on first real
hardware attempt:
- **`--t_cam_imu` wasn't given a real extrinsic** (§2.4) — prediction
  accuracy is directly sensitive to this, since `imu.py`'s lever-arm
  simplification already assumes the ROTATION part is correct.
- **Real BMI085 noise is worse than the synthetic gate's assumptions.**
  `config.py`'s `imu.noise_gyro`/`imu.noise_accel` are datasheet
  fallbacks — `record.py`'s docstring mentions a static-camera
  calibration recording gives better numbers. If prediction quality is
  poor specifically during otherwise-easy motion, measuring real noise
  characteristics (Allan variance or even just a static-recording std)
  and updating these config values is the first thing to try before
  suspecting a code bug.
- **Timestamps aren't actually on one clock.** Confirm
  `global_time_enabled` genuinely took effect — `record.py`'s own
  comment calls this "critical for IMU." If accel and gyro timestamps
  don't share a clock with the video frames, `imu.synchronize()`'s
  interpolation and the whole preintegration segment's timing become
  meaningless even though nothing crashes.

### 3.5 — `local_inertial_bundle_adjust` is slow or seems to hang again

Should not happen after Bug 4's fix, but if it does: the sparsity
pattern in that function is HAND-BUILT (not auto-derived), so any
future change to the parameter layout (`n_state_per_kf`, what a
residual depends on) must have a matching change to the `sparsity[...]
= True` assignments, or the pattern will either be wrong (silent
under-optimization — caught by `test_phase8_gate.py`'s Test 4 checking
convergence, not just speed) or missing entirely (back to the original
hang). If you touch this function's residual structure, re-run Test 4
specifically before trusting anything else.

### 3.6 — A keyframe has `.velocity is None` well into a run with `imu_initialized=True`

This would be Bug 3 recurring. Check that whatever new tracking
strategy or code path you added updates `frame.velocity` the same way
`track()`'s `if ok:` block now does (propagate via the IMU measurements
when available, not just when a specific strategy succeeded) — see
`tracking.py`'s Phase 8 comment on that block, and re-run
`test_phase8_gate.py`'s Test 5 (a direct, fast unit test for exactly
this) before chasing anything more elaborate.

---

## 4. Testing order

1. `test_phase8_gate.py` in full (§2.3) — covers the algorithm, all four
  bug regressions, and full-pipeline integration in one run.
2. A short (~30s) real hardware recording with `record.py --imu`, then
  played back through `run_dataset.py` with IMU wired (see that file's
  existing IMU support), checking the `[imu-init]` log line and
  `validate.py`'s health check.
3. Only once step 2 looks healthy: a real live `run_realsense() --imu`
  session, watching for the same `[imu-init]` success line and for
  `[TRACK]` output showing IMU-predicted frames succeeding (no direct
  log tag distinguishes Strategy 0 from Strategy 1/2 currently — if you
  need to confirm Strategy 0 specifically is firing on real hardware,
  a temporary print in `track()`'s Strategy 0 branch is the fastest way
  to check, not a permanent instrumentation addition).

## 5. What "success" looks like for this phase

1. `test_phase8_gate.py` passes in full, including the Phase 1-7
  regression.
2. On real hardware: `imu_initialized` flips `True` within the first
  few seconds of a normal facility walk (not requiring an unnatural
  "shake the camera" dance — some ordinary turning and starting/
  stopping should be enough, per `imu_init.py`'s gate).
3. Tracking survives a fast-rotation segment that previously caused loss
  (the original motivating scenario from Phase 5's hardware logs) more
  often than it did before this phase, on the same or a similar
  physical test.
4. No new crashes, hangs, or `validate.py` errors introduced by any of
  this on a real recording.



# Field Log — Phase 8 Hardware Verification (IMU-Based Motion Prediction)

Same rules as before: actual command output, not a prose summary. Phase
8 has NOT been tested on real hardware at all yet — everything verified
so far is synthetic (noise-free, perfectly synchronized by
construction). This is the first phase where real BMI085 noise and real
clock synchronization actually get exercised. Write "N/A" rather than
skip a field silently.

---

## 0. Environment

```bash
python3 -c "from run_slam import SLAMSystem; print('has _try_imu_init:', hasattr(SLAMSystem, '_try_imu_init'))"
python3 -c "from config import DEFAULTS as c; print(c['imu'])"
ls orbvoc_cache.npz || echo "MISSING -- run setup_vocabulary.py first"
```
**Paste output (confirm `has _try_imu_init: True`):**
```
[paste here]
```

---

## 1. Synthetic gate

```bash
python3 test_phase8_gate.py 2>&1 | tail -60
```
**Paste the full tail output, and how long the whole thing took:**
```
[paste here]
```
Time taken: **___ minutes** (10-15 min is normal, it re-runs the whole
Phase 1-7 regression internally)

---

## 2. Record a short IMU test clip

```bash
python3 record.py --out <dir> --imu --seconds 30
```
During the recording: some deliberate rotation AND some stop-start
translation in the first 2-3 seconds (needed for `imu_init.py`'s
observability gate — see `RUNBOOK_PHASE8.md` §3.3 if this fails).

**Paste the recording's console output, especially any T_cam_imu save
confirmation:**
```
[paste here]
```

---

## 3. Replay through run_dataset.py with IMU

```bash
python3 run_dataset.py --dataset <dir> [however IMU is wired into run_dataset.py currently -- check that file] 2>&1 | grep -E "\[imu|imu_init"
```
**Paste every `[imu-init]` log line:**
```
[paste here]
```
**Did initialization succeed?** [ ] Yes  [ ] No
**If no, what reason was given** (from `observability_gate`)?
```
[paste here]
```
**If yes, paste the recovered gravity/bias values:**
```
[paste here]
```

---

## 4. validate.py health check on this run

```python
import validate
report = validate.validate_atlas(slam.atlas)
print(report)
```
**Paste output:**
```
[paste here]
```
**Errors present?** [ ] Yes  [ ] No

---

## 5. Velocity coverage check

```python
kfs = slam.atlas.active_map.keyframes
n_no_vel = sum(1 for kf in kfs if kf.velocity is None)
print(f"{n_no_vel}/{len(kfs)} keyframes missing velocity")
```
**Paste output (expect 0/N once imu_initialized is True):**
```
[paste here]
```

---

## 6. Live realsense test with --imu

```bash
python3 run_slam.py --realsense --imu --t_cam_imu <dir>/calibration/T_cam_imu.txt --seconds 30
```
**Paste the console output, especially the `[imu-init]` line and final
summary:**
```
[paste here]
```

---

## 7. The motivating scenario: fast rotation / motion blur

Do a deliberately fast turn (the kind that caused tracking loss in
earlier phases' hardware logs) with `--imu` enabled, then repeat the
SAME physical motion with `--imu` NOT enabled (comment out the flag,
same room, same turn as close as you can repeat it).

**With --imu, tracking losses during the turn:**
```
[paste here]
```
**Without --imu, tracking losses during the same turn:**
```
[paste here]
```
**Did IMU prediction measurably help?** [ ] Yes  [ ] No  [ ] Inconclusive

---

## 8. Self-check before sending

* [ ] `_try_imu_init` confirmed real (§0)
* [ ] `test_phase8_gate.py` passes in full (§1)
* [ ] `imu_init` observability gate passed on a real recording (§3)
* [ ] `validate.py` shows zero errors (§4)
* [ ] Zero keyframes missing velocity once initialized (§5)
* [ ] Live `--realsense --imu` run completed without crashing (§6)
* [ ] Fast-rotation A/B comparison attempted (§7)

---

## 9. Anything else noticed

```
[paste here]
```

Both confirmed directly against the actual file trees, not from memory.

Phase 7 → Phase 8

Modified (5 code files):

run_slam.py — fixed the missing _try_imu_init method signature (Bug 1); wired --imu, --t_cam_imu flags and live D435i motion-stream capture into run_realsense() (Bug 2)
tracking.py — fixed the velocity-propagation single point of failure (Bug 3)
bundle_adjust.py — added the missing jac_sparsity pattern to local_inertial_bundle_adjust (Bug 4, the hang)
synthetic.py — added imu_ground_truth_sequence() and imu_visual_sequence() generators
PROGRESS.md — Phase 8 section appended

New (3 files):

test_phase8_gate.py
RUNBOOK_PHASE8.md
FIELD_LOG_PHASE8.md

Deleted: none

Phase 8 → Phase 9

Modified (3 code files):

run_slam.py — full iSAM2 wiring: per-keyframe backend selection, init-keyframe fix (Bug 2), _handle_loop_result rewritten for both intra-map and cross-map cases
config.py — bundle_adjust.backend default changed to "isam2", new isam2 config section, loop_closing.check_every_n_kf added
PROGRESS.md — Phase 9 section appended

New (5 files):

isam2_backend.py — the core new module
test_phase9_gate.py
RUNBOOK_PHASE9.md
FIELD_LOG_PHASE9.md
PHASE10_ARCHITECTURE_DENSE_RECONSTRUCTION.md — design doc, not yet built

Deleted: none

One thing worth flagging: bundle_adjust.py and tracking.py (Phase 8's own fixes) don't show up as changed again in Phase 9 — that's expected, Phase 9 only touched the files needed for the new backend and its wiring, and didn't need to revisit Phase 8's fixes.


# Phase 9 Runbook — Real-Time Incremental iSAM2 Back End

Companion to the earlier phase runbooks. Read §1 before debugging
anything related to `isam2_backend.py`, bundle-adjust behavior, or loop
closure — three real structural bugs were found building this phase,
none of them anticipated, all three the kind of thing that would
silently corrupt a live run rather than announce itself cleanly.

---

## 0. Context

Per `PHASE7_ARCHITECTURE_V2_REALTIME.md` section 2: `isam2_backend.py`
replaces `bundle_adjust_gtsam.py`'s windowed local BA and
`pose_graph.py`'s scipy loop correction with ONE continuously-maintained
GTSAM iSAM2 factor graph. `bundle_adjust.backend` defaults to `"isam2"`
now — the old paths are still in the codebase (selectable via config,
for comparison/fallback/debugging) but a fresh run uses the new one.

**Explicit scope boundary:** while IMU is initialized on a run, Phase
8's `local_inertial_bundle_adjust` keeps handling optimization instead
— unintegrated with iSAM2. IMU factors joining this same graph is Phase
10. If you're debugging an IMU-active run's optimization behavior,
you're looking at the wrong module; go to `bundle_adjust.py` and
`run_slam.py`'s IMU-active branch instead.

---

## 1. What changed, in one paragraph

One continuously-maintained iSAM2 graph now owns keyframe poses and map
points for the live, visual-only path — added to incrementally,
keyframe by keyframe, instead of re-solved over a sliding window every
time. Loop closure and map-merge correction are now just more factors
added to the same graph, not separate optimizers. Three real bugs were
found and fixed in the process, none of them design flaws so much as
integration gaps this project's own methodology exists to catch: a
keyframe sharing no map points with the graph so far was a genuinely
disconnected component (fixed with an always-present odometry factor to
the previous keyframe); the very first keyframe from `initializer.py`
never reached the backend at all (its own success branch returns before
the code that calls `add_keyframe()` is ever reached); and `ISAM2.
update()` turned out not to be transactional — a failed call can
partially commit values internally, which cascaded into permanent
failures for every later keyframe touching the same points until fixed
by reconciling against GTSAM's own `valueExists()` after any exception,
rather than assuming what did or didn't get committed.

---

## 2. Architecture map

```
┌─────────────────────────────────────────────────────────────────┐
│  NEW THIS PHASE                                                     │
│    isam2_backend.py    — Isam2Backend class: add_keyframe(),        │
│                         add_loop_factor(), absorb_map()              │
├─────────────────────────────────────────────────────────────────┤
│  WIRED IN                                                            │
│    run_slam.py         — one Isam2Backend per Atlas map              │
│                         (self.isam2_backends: Map.id -> backend),    │
│                         created lazily via _get_isam2_backend()      │
│    run_slam.py         — init branches (RGB-D + monocular) now       │
│                         explicitly call add_keyframe() before their  │
│                         early return (Bug 2's fix)                    │
│    run_slam.py         — _handle_loop_result(): intra-map loop uses  │
│                         add_loop_factor(); cross-map merge uses       │
│                         absorb_map() + add_loop_factor() bridging     │
│    config.py            — bundle_adjust.backend defaults to "isam2"; │
│                         new "isam2" section for tuning; loop_closing │
│                         .check_every_n_kf (default 1, was hardcoded  │
│                         "every 10th")                                 │
├─────────────────────────────────────────────────────────────────┤
│  RETIRED FROM THE LIVE PATH (kept in codebase, not deleted)          │
│    bundle_adjust_gtsam.py's local_bundle_adjust_gtsam()              │
│    pose_graph.py's optimize_pose_graph()                             │
│    -- select via bundle_adjust.backend = "gtsam" or "scipy" instead  │
│    of "isam2" if you need either for comparison/debugging            │
├─────────────────────────────────────────────────────────────────┤
│  UNCHANGED, STILL THE ACTIVE PATH WHEN IMU IS ON                     │
│    bundle_adjust.py's local_inertial_bundle_adjust (Phase 8)         │
├─────────────────────────────────────────────────────────────────┤
│  NOT DONE THIS PHASE (explicit, see PROGRESS.md)                     │
│    Fixed-lag marginalization -- graph is unbounded, deliberately,    │
│    per the "measure before adding complexity" plan                   │
│    IMU factors into this same graph -- Phase 10                       │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. Pre-flight checklist

**3.1 — Confirm the default backend.**
```bash
python3 -c "from config import load_config; print(load_config()['bundle_adjust']['backend'])"
```
Expect `isam2`. If you need the old windowed-BA path for comparison,
override in your own config rather than changing the shipped default.

**3.2 — Run the full gate before hardware.**
```bash
python3 test_phase9_gate.py
```
Slow (this project's gates keep getting slower as the regression list
grows — 10-20 minutes is normal now, since this transitively re-runs
Phase 6's own 247-frame fragmentation+merge scenario, itself now
exercising isam2 as a side effect of being the default backend).

**3.3 — If you add a new call site that creates keyframes or points,
check whether it needs a matching `add_keyframe()` call.** Bug 2 (the
init keyframe never reaching the backend) is the cautionary tale here:
any code path that creates a keyframe OUTSIDE the normal per-frame
`process()` flow — a new init path, a recovery mechanism, anything that
calls `mark_keyframe()` directly — needs to explicitly call
`self._get_isam2_backend(world_map).add_keyframe(keyframe, world_map)`
too, or that keyframe silently never enters the optimization graph.

---

## 4. Symptom → diagnosis → fix

### 4.1 — `IndeterminantLinearSystemException` on a specific keyframe (symbol `xNNN`) or landmark (symbol `lNNN`)

First, confirm this is being handled gracefully, not crashing the
pipeline: `add_keyframe()`'s try/except should catch this and print a
`[iSAM2] WARNING: ... update failed ...` line, and the pipeline should
keep running. If you see an UNCAUGHT traceback instead, that's a real
regression in the try/except itself — check `isam2_backend.py` catches
both `RuntimeError` AND `IndexError` (the "key already exists" failure
mode from Bug 3 surfaces as the latter, not the former).

If it's a `lNNN` (landmark) failure on a BRAND NEW point: check whether
the defensive filters (mono-only-first-factor, out-of-range position)
are somehow not catching this case — see `add_keyframe()`'s inline
comments for exactly what they check and why.

If it's an `xNNN` (keyframe pose) failure with points still reporting
as committed (check the `[iSAM2] WARNING` line's "kf committed=" /
"N/N new points committed" fields): this matches the known,
undiagnosed limitation from PROGRESS.md's Phase 9 section (observed
periodically on a highly repetitive synthetic scene, correlation with
keyframe culling checked and ruled out, best guess is a periodic-
relinearization interaction). It's handled gracefully (that keyframe's
pose just keeps its tracking-estimated value for that round), but if
this recurs at a MEANINGFUL RATE on real hardware (not just
occasionally), the tuning knobs to try first are `config.py`'s `isam2`
section: `relinearize_threshold` (raise it — less aggressive
relinearization) and `relinearize_skip` (raise it — relinearize less
often). Log every occurrence's frequency in `FIELD_LOG_PHASE9.md` before
changing anything, so there's a before/after comparison.

### 4.2 — "key already exists" (`IndexError`) appearing repeatedly, or the pipeline running noticeably slower over time with no new keyframes appearing to matter

This is Bug 3's cascade signature — if you see it, first confirm the
reconciliation fix is actually in place:
```bash
grep -n "valueExists" isam2_backend.py
```
Should appear in `add_keyframe`'s except block. If it's missing, this
exact bug has regressed — see PROGRESS.md's Phase 9 section for the
full mechanism (GTSAM's `ISAM2.update()` is NOT transactional; it can
partially commit values before throwing) and `test_reconciliation_
after_partial_commit_failure` in `test_phase9_gate.py` for the
regression test that catches this deterministically.

### 4.3 — A keyframe seems to be silently missing from the optimized trajectory

Check `slam.isam2_backends[world_map.id].kf_ids_in_graph` against
`world_map.keyframes` — a keyframe present in the latter but not the
former either (a) failed its `add_keyframe()` call and got logged as a
`[iSAM2] WARNING` (search backward in the log for its id), or (b) never
had `add_keyframe()` called for it at all, which is Bug 2's failure
shape recurring somewhere new — check §3.3.

### 4.4 — Map-merge correction doesn't seem to have connected the two former maps properly

Check that `_handle_loop_result`'s cross-map branch is calling
`absorb_map(absorbed_kfs, world_map, bridge_kf=matched_kf)` — NOT just
`absorb_map(absorbed_kfs, world_map)` without the `bridge_kf` argument.
Without it, the chronologically-first absorbed keyframe has no
connection to the survivor's graph at all (unless it happens to share
map points with something already there, which a genuine merge
scenario often doesn't guarantee) — see `absorb_map()`'s own docstring
and `test_absorb_map_bridges_two_backends` for the exact failure this
prevents.

### 4.5 — Timing / "is this actually real-time"

`Isam2Backend.last_update_seconds` is set after every `add_keyframe()`/
`add_loop_factor()` call (verbose mode logs it: `[iSAM2] kf N: ...
update Xms`). This project's own synthetic testing saw low-single-digit
milliseconds per update even past 150+ keyframes — but that's THIS
sandbox's CPU, not your Z440. Per
`PHASE7_ARCHITECTURE_V2_REALTIME.md` section 7's own plan: measure the
real number on real hardware before trusting any `--target-hz` choice.

---

## 5. Testing order

1. `test_phase9_gate.py` in full (§3.2).
2. A real facility recording through `run_dataset.py`, watching for
  `[iSAM2] WARNING` lines — log their frequency (§4.1's guidance) even
  if the pipeline handles them gracefully, since frequency is the
  actual signal for whether the undiagnosed periodic-failure pattern
  matters on real data.
3. `validate.py`'s health check on the resulting Atlas, same as every
  prior phase — this phase's own testing never saw it report an error
  even during the periodic-failure pattern, and that should stay true.
4. A real loop-closure and, if you can arrange it, a real map-merge
  scenario specifically, since those are the two paths with the most
  new code this phase (`add_loop_factor`, `absorb_map`).

## 6. What "success" looks like for this phase

1. `test_phase9_gate.py` passes in full, including the Phase 1-8
  regression.
2. On real hardware: per-keyframe `isam.update()` time stays comfortably
  under whatever `--target-hz` budget you're targeting (see the
  real-time architecture doc's own numbers as a starting comparison).
3. `[iSAM2] WARNING` lines, if any appear, stay rare and never cascade
  (repeated warnings on CONSECUTIVE keyframes touching the same points
  would indicate the reconciliation fix has regressed — see §4.2).
4. A real loop closure and a real map merge both visibly correct drift
  (compare trajectory before/after, same as prior phases' field logs
  already do) without the pipeline crashing or `validate.py` reporting
  errors.

# Field Log — Phase 9 Hardware Verification (Real-Time Incremental iSAM2 Back End)

Same rules as before: actual command output, not a prose summary. Phase
9 has NOT been tested on real hardware at all yet — everything verified
so far is synthetic. This phase found real, structural bugs purely from
synthetic testing (see `PROGRESS.md`'s Phase 9 section) — treat this
field log as the next opportunity to find whatever synthetic testing
couldn't surface, not a formality. Write "N/A" rather than skip a field
silently.

---

## 0. Environment

```bash
python3 -c "from config import load_config; print(load_config()['bundle_adjust']['backend'])"
python3 -c "from config import load_config; print(load_config()['isam2'])"
ls orbvoc_cache.npz || echo "MISSING -- run setup_vocabulary.py first"
```
**Paste output (confirm backend is "isam2"):**
```
[paste here]
```

---

## 1. Synthetic gate

```bash
python3 test_phase9_gate.py 2>&1 | tail -80
```
**Paste the full tail output, and how long the whole thing took:**
```
[paste here]
```
Time taken: **___ minutes**

---

## 2. `[iSAM2]` warning frequency on a real recording (the most important new diagnostic this phase)

Run a real facility walk through `run_dataset.py` (or live via
`run_slam.py --realsense`), and count every `[iSAM2] WARNING` line:

```bash
python3 run_dataset.py --dataset <dir> 2>&1 | grep "\[iSAM2\]" | tee isam2_warnings.log
wc -l isam2_warnings.log
```
**Total warning count:** ______  **Total keyframes created:** ______
**Warning rate:** ______%

**Paste every distinct warning message (dedupe by symbol type, not
exact id):**
```
[paste here]
```

**Did any warnings appear on CONSECUTIVE keyframes touching related
points (the cascade signature — see RUNBOOK_PHASE9.md §4.2)?**
[ ] Yes  [ ] No — if yes, this needs investigation before trusting this
run's trajectory at all; the reconciliation fix should prevent this,
so a real cascade here would be a regression.

---

## 3. Timing (the real bring-up benchmark this phase's own architecture doc calls for)

```python
# after a real run, or add temporary logging in run_slam.py's isam2 branch
import numpy as np
times = [...]  # collect isam2.last_update_seconds per keyframe if you added logging
print(f"mean={np.mean(times)*1000:.1f}ms median={np.median(times)*1000:.1f}ms "
     f"max={np.max(times)*1000:.1f}ms  n={len(times)}")
```
**Paste output:**
```
[paste here]
```
**Does this comfortably fit your target processed-frame rate** (see
`PHASE7_ARCHITECTURE_V2_REALTIME.md` section 7)? [ ] Yes  [ ] No

---

## 4. validate.py health check

```python
import validate
report = validate.validate_atlas(slam.atlas)
print(report)
```
**Paste output:**
```
[paste here]
```
**Errors present?** [ ] Yes  [ ] No

---

## 5. Real loop closure

```bash
python3 run_dataset.py --dataset <dir showing a real revisit> 2>&1 | grep "\[LOOP\]"
```
**Paste every `[LOOP]` line:**
```
[paste here]
```
**Did it say "CORRECTED via iSAM2" or "iSAM2 update FAILED"?**
```
[paste here]
```

---

## 6. Real map merge (if you can arrange a scenario that fragments and later reunites)

**Paste the `[LOOP] ... (CROSS-MAP) ... -> MERGED` line:**
```
[paste here]
```
**Trajectory sanity check — does the merged map look geometrically
consistent (no visible seam/kink at the merge point)?**
[ ] Yes  [ ] No  [ ] N/A (no merge scenario available this session)

---

## 7. Self-check before sending

* [ ] Backend confirmed as "isam2" (§0)
* [ ] `test_phase9_gate.py` passes in full (§1)
* [ ] `[iSAM2]` warning rate logged, even if zero (§2)
* [ ] No cascade signature (consecutive-keyframe warnings) observed (§2)
* [ ] Real per-keyframe timing measured, not just this sandbox's number (§3)
* [ ] `validate.py` shows zero errors (§4)

---

## 8. Anything else noticed

```
[paste here]
```



"""
run_slam.py — the orchestrator.

Replaces: mono_video.cc + System.cc's thread launching.
Real ORB-SLAM3 runs Tracking / LocalMapping / LoopClosing on three parallel
threads. We run them sequentially in one loop — simpler to follow, and the
result is identical for offline processing.

Two input modes:
  --realsense       live D435 (RGB-D, uses depth for instant init)
  --frames <dir>    replay pre-extracted PNG frames (monocular)

Usage:
  python3 run_slam.py --realsense --seconds 30
  python3 run_slam.py --frames ~/orb_scratch/IMG_1112 --calib calibration/x.json
"""

import argparse
import glob
import os
import sys
import time

import cv2
import numpy as np
import matplotlib
matplotlib.use("Agg")
import matplotlib.pyplot as plt

from camera import Camera
from extractor import Extractor
from matcher import Matcher
from frame import Frame
from atlas import Atlas
from tracking import Tracking
from local_mapping import LocalMapping
from orb_vocabulary import ORBVocabulary
from keyframe_database import KeyFrameDatabase
import relocalization
import pose_graph
import merge_maps
import fusion
import covisibility
from loop_closing import LoopClosing
import initializer
from bundle_adjust import local_bundle_adjust, local_inertial_bundle_adjust, pose_only_optimize
# Phase 2: GTSAM backend is optional -- only imported if config actually
# requests it, so `gtsam` is not a hard dependency for anyone still on the
# scipy backend. See bundle_adjust_gtsam.py's docstring for why this
# exists and PROGRESS.md for the three bugs found verifying it.
try:
    from bundle_adjust_gtsam import local_bundle_adjust_gtsam
except ImportError:
    local_bundle_adjust_gtsam = None
import imu
import imu_init
from isam2_backend import Isam2Backend
from config import load_config


class SLAMSystem:
    def __init__(self, camera, use_depth=False, verbose=True, use_imu=False, config_path=None):
        self.camera = camera
        self.use_depth = use_depth
        self.verbose = verbose
        self.use_imu = use_imu
        self.cfg = load_config(config_path)

        # BUGFIX: config.py was loaded (self.cfg) but every module below
        # used to be constructed with hardcoded defaults instead of cfg's
        # values -- e.g. cfg["loop_closing"]["vocab_words"]=10000 existed on
        # disk but Vocabulary(n_words=64) below ignored it completely, and
        # nothing in cfg["extractor"]/["tracking"]/["local_mapping"] had any
        # effect at all. Wired through properly now so config.yaml edits
        # actually change behavior. See PROGRESS.md.
        self.extractor = Extractor(**self.cfg["extractor"])
        self.matcher = Matcher()
        self.atlas = Atlas()
        self.tracking = Tracking(camera, self.extractor, self.matcher,
                                 self.atlas.active_map, **self.cfg["tracking"])
        self.local_mapping = LocalMapping(camera, self.matcher,
                                          self.atlas.active_map,
                                          extractor=self.extractor,
                                          **self.cfg["local_mapping"])
        # Shared covisibility graph: LocalMapping rebuilds this dict IN
        # PLACE (see covisibility.py) after every keyframe, so Tracking
        # always sees the latest version through this one reference.
        self.tracking.covis_graph = self.local_mapping.covis_graph
        # Phase 5: real ORBvoc-format vocabulary replaces the KMeans
        # placeholder entirely -- see orb_vocabulary.py's module docstring
        # for why, and setup_vocabulary.py for the one-time fetch script.
        # Loading is NOT wrapped in a silent try/except: a facility-
        # mapping run without a real vocabulary (BoW is an explicit hard
        # requirement for this project) should fail loudly and tell the
        # person exactly what to run, not silently degrade to something
        # worse. If you deliberately want to run without it for a quick
        # test, catch FileNotFoundError yourself around SLAMSystem(...).
        self.vocab = ORBVocabulary()
        self.vocab.load()
        self.keyframe_db = KeyFrameDatabase(self.vocab)
        self.loop_closer = LoopClosing(camera, self.matcher, self.vocab,
                                       keyframe_db=self.keyframe_db,
                                       atlas=self.atlas,
                                       min_keyframe_gap=self.cfg["loop_closing"]["min_keyframe_gap"],
                                       consistency_checks=self.cfg["loop_closing"]["consistency_checks"])

        # ── Visual-inertial state (Phase 3/4) ───────────────────────────
        # imu_preint: the RUNNING preintegration accumulator since the last
        # keyframe. Created once the first keyframe exists, reset every
        # time a new keyframe is marked (see process() below). bias_* are
        # carried forward as constants once imu_init.py succeeds (see that
        # module's docstring for why bias isn't re-estimated online here).
        self.imu_preint = None
        self.imu_raw_buffer = []     # accumulated synchronized [t,gx,gy,gz,ax,ay,az] rows
        self._last_imu_t = None
        self.bias_gyro = np.zeros(3)
        self.bias_accel = np.zeros(3)
        self.last_ok_timestamp = None
        # PHASE 8: raw per-stream buffers for the LIVE realsense capture
        # path (run_realsense()) -- accel and gyro arrive as separate
        # async streams at different native rates, so they're buffered
        # here and synchronized (imu.synchronize()) once per process()
        # call rather than per-sample. Unused/empty for any other input
        # path (run_dataset.py already hands over pre-synchronized rows
        # loaded straight from a recording's imu.csv).
        self._live_accel_buf = []
        self._live_gyro_buf = []

        self.frames = []
        self.init_candidate = None
        # PHASE 9: one Isam2Backend per Atlas map (mirrors the existing
        # per-map tracking.set_map()/local_mapping.set_map() pattern),
        # created lazily on that map's first keyframe. Only used for the
        # VISUAL-ONLY path (backend config == "isam2" and IMU not yet
        # initialized on this run) -- see the per-keyframe block in
        # process() and this class's docstring-level notes on why IMU
        # integration into this same graph is explicitly OUT of scope
        # for this phase (Phase 8's local_inertial_bundle_adjust keeps
        # handling the IMU-active case, unintegrated, until a later
        # phase joins them).
        self.isam2_backends = {}   # Map.id -> Isam2Backend
        self.consecutive_lost = 0
        self.stats = {'tracked': 0, 'lost': 0, 'keyframes': 0,
                      'points_created': 0, 'points_culled': 0, 'loops': 0,
                      'points_fused': 0, 'keyframes_culled': 0,
                      'imu_init_attempts': 0, 'relocalizations': 0}

    def process(self, image, timestamp, depth_image=None, imu_samples=None):
        """
        imu_samples: optional (K,7) array of synchronized [t,gx,gy,gz,ax,ay,az]
        rows (see imu.py) covering the interval since the previous process()
        call. Already rotated into the camera frame. Ignored entirely
        unless use_imu=True.
        """
        frame = Frame(image, timestamp, self.camera, self.extractor,
                      depth_image=depth_image)
        
        # ── Diagnostic output immediately after creating each Frame ──
        # BUGFIX: Frame has no `valid_depth_count` attribute -- this used to
        # silently print 0 forever via getattr's fallback. n_valid_depths()
        # is the real accessor (see frame.py).
        if self.verbose:
            print(f"[RGBD] Frame {frame.id}: keypoints={len(frame.keypoints)}, "
                  f"valid_depth={frame.n_valid_depths()}")

        self.frames.append(frame)
        world_map = self.atlas.active_map

        if self.use_imu and imu_samples is not None and len(imu_samples) > 0:
            self._integrate_imu(imu_samples)

        # ── initialisation ───────────────────────────────────────────────
        if self.tracking.state == "NOT_INITIALIZED":
            if self.use_depth:
                ok, new_pts, fail_reason = initializer.init_rgbd(
                    frame, world_map, min_points=self.cfg["initializer"]["rgbd_min_points"])
                if ok:
                    self.tracking.state = "OK"
                    self.tracking.last_frame = frame
                    self.tracking.mark_keyframe(frame)
                    self.local_mapping.recent_points.extend(new_pts)
                    self.stats['keyframes'] += 1
                    self.stats['points_created'] += len(new_pts)
                    self.last_ok_timestamp = timestamp
                    self.keyframe_db.add_keyframe(frame)   # Phase 5
                    if self.use_imu:
                        self._start_imu_segment()
                    # PHASE 9 BUGFIX: the init keyframe used to never
                    # reach isam2_backend.py at all -- this whole branch
                    # `return`s below, before the ordinary per-keyframe
                    # block (where add_keyframe() normally gets called)
                    # is ever reached. That meant the FIRST keyframe --
                    # which local_mapping/init_rgbd anchors most of the
                    # map's early points to -- was silently absent from
                    # the graph, so whichever keyframe reached
                    # add_keyframe() FIRST became the gauge anchor
                    # instead, anchored at ITS OWN (not the true origin)
                    # pose, with every point the true first keyframe
                    # created inserted fresh, single-factor, in one large
                    # batch. Found via a real IndeterminantLinearSystem-
                    # Exception on a realistic multi-fragment synthetic
                    # sequence -- see PROGRESS.md's Phase 9 section.
                    if self.cfg["bundle_adjust"].get("backend", "scipy") == "isam2":
                        self._get_isam2_backend(world_map).add_keyframe(frame, world_map)
                    self._log(f"[init] RGB-D success at frame {frame.id} "
                              f"({len(new_pts)} points)")
                else:
                    self._log(f"[init] RGB-D failed at frame {frame.id}: {fail_reason}")
            else:
                if self.init_candidate is None:
                    self.init_candidate = frame
                    return frame
                ok, new_pts = initializer.init_monocular(
                    self.init_candidate, frame, self.matcher,
                    self.camera, world_map)
                if ok:
                    self.tracking.state = "OK"
                    self.tracking.last_frame = frame
                    self.tracking.mark_keyframe(frame)
                    self.local_mapping.recent_points.extend(new_pts)
                    self.stats['keyframes'] += 2
                    self.stats['points_created'] += len(new_pts)
                    self.last_ok_timestamp = timestamp
                    self.keyframe_db.add_keyframe(self.init_candidate)   # Phase 5
                    self.keyframe_db.add_keyframe(frame)                 # Phase 5
                    if self.use_imu:
                        self._start_imu_segment()
                    # PHASE 9 BUGFIX: same gap as the RGB-D init branch
                    # above -- both init keyframes must be added, in
                    # chronological order, so the FIRST one (not
                    # whichever keyframe happens to reach the ordinary
                    # per-keyframe block first) becomes the gauge anchor.
                    if self.cfg["bundle_adjust"].get("backend", "scipy") == "isam2":
                        isam2 = self._get_isam2_backend(world_map)
                        isam2.add_keyframe(self.init_candidate, world_map)
                        isam2.add_keyframe(frame, world_map)
                    self._log(f"[init] mono success at frame {frame.id} "
                              f"({len(new_pts)} points)")
                else:
                    self.init_candidate = frame   # slide the window forward
                    if frame.id % 30 == 0:
                        self._log(f"[init] waiting for parallax... frame {frame.id}")
            return frame

        # ── tracking ─────────────────────────────────────────────────────
        ok = self.tracking.track(frame, imu_preint=self.imu_preint if self.use_imu else None)
        if ok:
            self.last_ok_timestamp = timestamp
        if not ok:
            self.stats['lost'] += 1
            self.consecutive_lost += 1
            self._log(f"[track] LOST at frame {frame.id} "
                      f"(consecutive: {self.consecutive_lost})")

            # Atlas behaviour: after sustained loss, abandon and start fresh.
            # With IMU initialized, give the pipeline a TIME-based grace
            # window instead of a fixed frame count -- tracking.py already
            # keeps propagating frame.pose via IMU alone during this
            # window (state RECENTLY_LOST), so a few seconds of bad visual
            # conditions (motion blur, a blank wall) can recover instead of
            # immediately abandoning the map.
            elapsed_lost = timestamp - self.last_ok_timestamp if self.last_ok_timestamp is not None else 0.0
            if self.use_imu and self.tracking.imu_initialized:
                give_up = (elapsed_lost >= self.cfg["imu"]["recently_lost_max_seconds"]
                           and self.consecutive_lost >= 3)
            else:
                give_up = self.consecutive_lost >= 10

            # Phase 5: attempt relocalization BEFORE giving up. This is
            # the direct fix for the original "no Relocalization()"
            # gap flagged from this project's very first forensic pass --
            # every sustained tracking loss used to permanently fragment
            # the Atlas with zero recovery attempt. Tried on every LOST
            # frame, not just once give_up triggers: a successful match
            # here means we never fragment in the first place, whether
            # the recognized place is in the CURRENTLY active map (no
            # fragmentation ever happens) or an older, already-abandoned
            # one (Atlas resumes it instead of starting yet another
            # fragment -- see relocalization.py's docstring for why this
            # switches maps rather than attempting a full merge, which is
            # real, separate work deferred to Phase 6).
            exclude_ids = {self.tracking.last_keyframe.id} if self.tracking.last_keyframe else None
            relocalized, matched_map = relocalization.try_relocalize(
                frame, self.keyframe_db, self.atlas, self.camera, self.matcher,
                min_inliers=self.cfg["relocalization"]["min_inliers"],
                top_n_candidates=self.cfg["relocalization"]["top_n_candidates"],
                exclude_ids=exclude_ids)

            if relocalized:
                self._log(f"[reloc] recovered at frame {frame.id} into "
                          f"map {matched_map.id} after {self.consecutive_lost} lost frames")
                self.tracking.set_map(matched_map)
                self.local_mapping.set_map(matched_map)
                self.tracking.covis_graph = self.local_mapping.covis_graph
                self.tracking.state = "OK"
                self.tracking.last_frame = frame
                self.tracking.velocity = None   # motion model has no basis to trust yet
                self.consecutive_lost = 0
                self.last_ok_timestamp = timestamp
                self.stats['relocalizations'] = self.stats.get('relocalizations', 0) + 1
                return frame

            if give_up:
                self._log(f"[atlas] starting NEW MAP after "
                          f"{self.consecutive_lost} lost frames ({elapsed_lost:.2f}s)")
                new_map = self.atlas.start_new_map()
                self.tracking.set_map(new_map)
                self.local_mapping.set_map(new_map)
                self.tracking.covis_graph = self.local_mapping.covis_graph
                self.tracking.state = "NOT_INITIALIZED"
                self.tracking.last_frame = None
                self.tracking.last_keyframe = None
                self.tracking.velocity = None
                self.tracking.imu_initialized = False
                self.init_candidate = None
                self.consecutive_lost = 0
                self.imu_preint = None
            return frame

        self.consecutive_lost = 0
        self.stats['tracked'] += 1
        pose_only_optimize(frame, world_map, self.camera)

        # ── keyframe -> local mapping ────────────────────────────────────
        if self.tracking.needs_new_keyframe(frame):
            new_pts, n_culled, n_fused = self.local_mapping.process_new_keyframe(
                frame, use_depth=self.use_depth, depth_image=depth_image)
            self.tracking.mark_keyframe(frame)
            self.stats['keyframes'] += 1
            self.stats['points_created'] += len(new_pts)
            self.stats['points_culled'] += n_culled
            self.stats['points_fused'] += n_fused
            self.keyframe_db.add_keyframe(frame)   # Phase 5

            if self.use_imu:
                self._finalize_imu_segment(frame)

            # KeyFrameCulling: drop redundant keyframes so covisibility
            # rebuilds / BA windows / local-map matching don't grow forever
            # over a facility-length walk. Cheap enough to run every
            # keyframe; internally protects the most recent few.
            #
            # SKIPPED when IMU is active: each keyframe's imu_preint spans
            # from its IMMEDIATE predecessor at recording time. If that
            # predecessor gets culled, the chain imu_init.py and
            # local_inertial_bundle_adjust walk (consecutive keyframes in
            # world_map.keyframes) would silently desync from what each
            # segment actually covers. Fixing that properly means re-
            # concatenating preintegration segments across a cull, which
            # is real work deferred for now -- see PROGRESS notes.
            if not self.use_imu:
                n_kf_culled, culled_ids = self.local_mapping.cull_keyframes()
                self.stats['keyframes_culled'] += n_kf_culled
                for kf_id in culled_ids:   # Phase 5: keep the inverted index consistent
                    self.keyframe_db.remove_keyframe(kf_id)

            # PHASE 9: real-time back end. The windowed local_bundle_
            # adjust_gtsam()/local_bundle_adjust() batch calls below are
            # RETIRED from this live per-keyframe path for the visual-
            # only case -- replaced by one continuous incremental iSAM2
            # graph (isam2_backend.py), which is what the real-time
            # architecture reframe (PHASE7_ARCHITECTURE_V2_REALTIME.md)
            # calls for and is now this project's default. They're kept
            # in the codebase (not deleted) for comparison/fallback --
            # select the old path via config["bundle_adjust"]["backend"]
            # = "gtsam" or "scipy" instead of "isam2" if you need it.
            #
            # SCOPE BOUNDARY (explicit, not an oversight): IMU factors
            # joining this SAME graph is deliberately NOT done this
            # phase -- see PHASE7_ARCHITECTURE_V2_REALTIME.md's
            # Workstream E, still future work. While IMU is active on a
            # given run, Phase 8's local_inertial_bundle_adjust keeps
            # handling optimization instead, exactly as it already did --
            # the iSAM2 backend simply stops receiving new keyframes for
            # that map once IMU takes over, so the two never fight over
            # the same pose variables.
            n_kf = world_map.n_keyframes()
            if self.use_imu and not self.tracking.imu_initialized:
                self._try_imu_init(world_map)

            backend = self.cfg["bundle_adjust"].get("backend", "scipy")
            use_isam2 = (backend == "isam2" and
                        not (self.use_imu and self.tracking.imu_initialized))

            if self.use_imu and self.tracking.imu_initialized:
                if n_kf >= 3:
                    local_inertial_bundle_adjust(
                        world_map, self.camera, gravity=self.tracking.gravity,
                        bias_gyro=self.bias_gyro, bias_accel=self.bias_accel,
                        window=self.cfg["imu"]["ba_window"], verbose=self.verbose)
            elif use_isam2:
                isam2 = self._get_isam2_backend(world_map)
                n_pts, n_obs, dt = isam2.add_keyframe(frame, world_map)
                if self.verbose:
                    print(f"[iSAM2] kf {frame.id}: +{n_pts} new pts, "
                         f"+{n_obs} obs, update {dt*1000:.1f}ms "
                         f"({isam2.n_updates} total updates)")
            elif n_kf >= 3:
                # gtsam/scipy fallback comparison path (backend != "isam2").
                if backend == "gtsam" and local_bundle_adjust_gtsam is not None:
                    local_bundle_adjust_gtsam(world_map, self.camera,
                                              window=self.cfg["bundle_adjust"]["window"],
                                              max_iter=self.cfg["bundle_adjust"]["max_iter"],
                                              min_obs_to_optimize=self.cfg["bundle_adjust"]["min_obs_to_optimize"],
                                              huber_f_scale=self.cfg["bundle_adjust"]["huber_f_scale"],
                                              verbose=self.verbose)
                else:
                    if backend == "gtsam" and self.verbose:
                        print("[BA] config requested gtsam backend but gtsam "
                             "is not installed -- falling back to scipy")
                    local_bundle_adjust(world_map, self.camera,
                                        window=self.cfg["bundle_adjust"]["window"],
                                        max_iter=self.cfg["bundle_adjust"]["max_iter"],
                                        min_obs_to_optimize=self.cfg["bundle_adjust"]["min_obs_to_optimize"],
                                        huber_f_scale=self.cfg["bundle_adjust"]["huber_f_scale"],
                                        verbose=self.verbose)

            # ── loop closing ─────────────────────────────────────────────
            # Phase 6: loop closing is now Atlas-wide (keyframe_database.py)
            # with 3D-3D geometric verification (loop_verification.py),
            # and a confirmed loop is ACTUALLY CORRECTED, not just logged
            # -- the "NOT corrected" era ends here. Two distinct cases,
            # branched on whether the matched keyframe is in the SAME map
            # (intra-map: pose_graph.py redistributes the loop error
            # across the path) or a DIFFERENT, previously-fragmented map
            # (cross-map: merge_maps.py actually folds it back in, which
            # relocalization.py's map-switching alone never did).
            #
            # Extracted into _handle_loop_result() (see below) rather than
            # inlined here: test_phase6_gate.py's end-to-end test needs to
            # invoke this exact logic directly, independent of whether the
            # n_kf%10==0 cadence happens to land on the right keyframe in
            # a finite synthetic sequence -- the cadence itself is a
            # legitimate production design choice (bounding how often an
            # expensive Atlas-wide check runs), not something a
            # correctness test should be at the mercy of.
            # PHASE 9: cadence is now configurable
            # (loop_closing.check_every_n_kf), defaulting to 1 (every
            # keyframe) -- the real-time reframe's own justification
            # (PHASE7_ARCHITECTURE_V2_REALTIME.md sec 3/7): a slow-moving
            # robot's keyframes are seconds apart, not the tight-real-
            # time cadence the old hardcoded "every 10th" was designed
            # for, so checking every keyframe is affordable. The
            # `n_kf >= 20` warm-up guard stays -- no point checking
            # before there's a meaningfully large map to match against.
            check_every = self.cfg["loop_closing"].get("check_every_n_kf", 1)
            if n_kf % check_every == 0 and n_kf >= 20:
                result = self.loop_closer.detect_loop(frame, world_map)
                if result is not None:
                    world_map = self._handle_loop_result(frame, world_map, result)
        return frame

    # ── IMU orchestration (Phase 3/4) ───────────────────────────────────

    def _integrate_imu(self, imu_samples):
        """Append raw synchronized samples to the running buffer (used by
        imu_init's observability gate) and integrate them into the RUNNING
        preintegration segment since the last keyframe, if one exists yet."""
        self.imu_raw_buffer.extend(imu_samples.tolist())
        if self.imu_preint is None:
            return
        t_prev = self._last_imu_t
        for row in imu_samples:
            t, gx, gy, gz, ax, ay, az = row
            if t_prev is not None:
                self.imu_preint.integrate_sample([gx, gy, gz], [ax, ay, az], t - t_prev)
            t_prev = t
        self._last_imu_t = t_prev

    def _get_isam2_backend(self, world_map):
        """PHASE 9: get-or-create this map's Isam2Backend instance."""
        backend = self.isam2_backends.get(world_map.id)
        if backend is None:
            isam2_cfg = self.cfg.get("isam2", {})
            backend = Isam2Backend(
                self.camera,
                virtual_baseline=self.cfg["bundle_adjust"].get("virtual_baseline", 0.05),
                huber_f_scale=self.cfg["bundle_adjust"]["huber_f_scale"],
                min_obs_to_optimize=self.cfg["bundle_adjust"]["min_obs_to_optimize"],
                relinearize_threshold=isam2_cfg.get("relinearize_threshold", 0.1),
                relinearize_skip=isam2_cfg.get("relinearize_skip", 1),
                anchor_sigma_pos=isam2_cfg.get("anchor_sigma_pos", 1e-3),
                anchor_sigma_rot=isam2_cfg.get("anchor_sigma_rot", 1e-2),
                odom_sigma_pos=isam2_cfg.get("odom_sigma_pos", 0.05),
                odom_sigma_rot=isam2_cfg.get("odom_sigma_rot", 0.05))
            self.isam2_backends[world_map.id] = backend
        return backend

    def _start_imu_segment(self):
        """Begin accumulating a fresh preintegration segment from the
        just-created (first) keyframe forward."""
        self.imu_preint = imu.Preintegration(
            self.bias_gyro, self.bias_accel,
            noise_gyro=self.cfg["imu"]["noise_gyro"],
            noise_accel=self.cfg["imu"]["noise_accel"])
        self._last_imu_t = None

    def _finalize_imu_segment(self, keyframe):
        """Attach the just-completed segment to the new keyframe and start
        the next one."""
        if self.imu_preint is None:
            self._start_imu_segment()
            return
        keyframe.imu_preint = self.imu_preint
        keyframe.bias_gyro = self.bias_gyro.copy()
        keyframe.bias_accel = self.bias_accel.copy()
        self._start_imu_segment()

    def _handle_loop_result(self, frame, world_map, result):
        """
        Phase 6: apply a CONFIRMED loop-closure result -- either intra-map
        pose-graph correction or cross-map merging. Extracted from
        process()'s inline loop-closing block specifically so
        test_phase6_gate.py can invoke this exact logic directly,
        independent of whether the production n_kf%10==0 cadence happens
        to land on the right keyframe within a finite synthetic test
        sequence (see that file's test_end_to_end_merge for the full
        reasoning). Returns the (possibly updated, if a merge happened)
        world_map -- callers MUST use the returned value, not assume the
        one they passed in is still current.
        """
        matched_kf, matched_map, sim, inliers, R, t, s = result
        drift = self.loop_closer.measure_drift(frame, matched_kf)
        self.stats['loops'] += 1

        # PHASE 9: same "is iSAM2 the active real-time back end right
        # now" check used in process()'s per-keyframe block -- IMU-active
        # runs still fall back to pose_graph.py's scipy correction (see
        # that block's SCOPE BOUNDARY comment for why).
        use_isam2 = (self.cfg["bundle_adjust"].get("backend", "scipy") == "isam2" and
                    not (self.use_imu and self.tracking.imu_initialized))

        if matched_map is world_map:
            loop_edge = pose_graph.compute_loop_edge(
                frame, matched_kf, self.matcher, self.camera, world_map)
            if loop_edge is not None:
                T_rel, n_pnp_inliers = loop_edge
                if use_isam2:
                    isam2 = self._get_isam2_backend(world_map)
                    dt = isam2.add_loop_factor(
                        matched_kf, frame, T_rel, loop_weight=min(100.0, n_pnp_inliers),
                        world_map=world_map)
                    # Loop-seam fusion, same as pose_graph.py always did
                    # (these two keyframes were never naturally covisible
                    # before the loop closed) -- iSAM2 corrects POSES via
                    # the factor above, but doesn't discover duplicate
                    # points across the seam by itself.
                    n_levels = self.extractor.nlevels
                    scale_factor = self.extractor.scale_factor
                    ad_hoc_graph = {matched_kf.id: {frame.id: 999}, frame.id: {matched_kf.id: 999}}
                    n_fused = fusion.search_in_neighbors(frame, world_map, ad_hoc_graph, self.camera,
                                                         n_levels=n_levels, scale_factor=scale_factor)
                    covisibility.build_covisibility_graph(world_map, graph=self.local_mapping.covis_graph)
                    self._log(f"[LOOP] kf {frame.id} <-> kf {matched_kf.id} "
                              f"(same map) | sim={sim:.3f} inliers={inliers} | "
                              f"drift={drift:.3f}m -> "
                              f"{'CORRECTED via iSAM2 (' + str(round(dt*1000,1)) + 'ms, ' + str(n_fused) + ' fused)' if dt is not None else 'iSAM2 update FAILED (see [iSAM2] WARNING above) -- pose left uncorrected this round'}")
                else:
                    pg_stats = pose_graph.optimize_pose_graph(
                        world_map, frame, matched_kf, T_rel,
                        loop_weight=min(100.0, n_pnp_inliers),
                        camera=self.camera, covis_graph=self.local_mapping.covis_graph,
                        extractor=self.extractor, verbose=self.verbose)
                    self._log(f"[LOOP] kf {frame.id} <-> kf {matched_kf.id} "
                              f"(same map) | sim={sim:.3f} inliers={inliers} | "
                              f"drift={drift:.3f}m -> CORRECTED "
                              f"({pg_stats['n_keyframes_corrected'] if pg_stats else 0} kfs, "
                              f"{pg_stats['n_points_corrected'] if pg_stats else 0} pts)")
            else:
                self._log(f"[LOOP] kf {frame.id} <-> kf {matched_kf.id} "
                          f"(same map) | drift={drift:.3f}m -> correction "
                          f"attempted but compute_loop_edge found too few "
                          f"inliers, skipped")
            return world_map

        merge_stats = merge_maps.merge_maps(
            self.atlas, matched_map, world_map, R, t, s,
            anchor_kf_a=frame, anchor_kf_b=matched_kf,
            camera=self.camera, matcher=self.matcher,
            extractor=self.extractor,
            covis_graph=self.local_mapping.covis_graph,
            verbose=self.verbose)
        # world_map (the just-absorbed fragment) no longer exists as a
        # separate map -- everything downstream of this point needs to
        # operate on matched_map, the survivor. Mirrors relocalization.py's
        # own post-switch bookkeeping exactly.
        absorbed_kfs = list(world_map.keyframes)   # merge_maps leaves this list intact -- see its docstring
        world_map = matched_map
        self.tracking.set_map(world_map)
        self.local_mapping.set_map(world_map)
        self.tracking.covis_graph = self.local_mapping.covis_graph

        if use_isam2:
            # PHASE 9: bring the absorbed map's keyframes into the
            # SURVIVOR'S iSAM2 graph -- absorb_map() connects the first
            # one via bridge_kf (matched_kf, already in this graph) so
            # it's never left as a disconnected component (see
            # isam2_backend.py's absorb_map() docstring), then this
            # explicit add_loop_factor() call afterward adds the REAL,
            # PnP-measured transform between the actual matched pair as
            # a tighter constraint -- the bridge only prevents a crash,
            # this is what actually pulls the seam tight.
            isam2 = self._get_isam2_backend(world_map)
            isam2.absorb_map(absorbed_kfs, world_map, bridge_kf=matched_kf)
            # anchor_kf_a (frame) and anchor_kf_b (matched_kf) both now
            # live in world_map's coordinate frame post-merge.
            T_rel_bridge = np.linalg.inv(matched_kf.pose) @ frame.pose
            isam2.add_loop_factor(matched_kf, frame, T_rel_bridge,
                                  loop_weight=min(100.0, inliers), world_map=world_map)

        self._log(f"[LOOP] kf {frame.id} <-> kf {matched_kf.id} "
                  f"(CROSS-MAP) | sim={sim:.3f} inliers={inliers} | "
                  f"drift={drift:.3f}m -> MERGED "
                  f"({merge_stats['n_keyframes_after']} kfs, "
                  f"{merge_stats['n_points_after']} pts, "
                  f"scale={merge_stats['scale_applied']:.3f})")
        return world_map

    def _try_imu_init(self, world_map):
        """
        PHASE 8 BUGFIX: this method's docstring and body existed in the
        code, but the `def _try_imu_init(self, world_map):` signature line
        itself was MISSING -- the entire block was dead, dangling code
        sitting after _handle_loop_result's own `return`, not a real
        method. `hasattr(SLAMSystem, '_try_imu_init')` returned False.
        The call site in process() (`if self.use_imu and not self.
        tracking.imu_initialized: self._try_imu_init(world_map)`) would
        raise AttributeError on the very first keyframe processed with
        use_imu=True -- meaning IMU initialization has never successfully
        run, in any test, on any recording, ever, in this project's
        history. Nothing caught this because no gate test and no field
        log ever actually set use_imu=True end-to-end (see PROGRESS.md's
        Phase 8 section for the full account and test_phase8_gate.py for
        the regression test that exercises this directly).

        Attempt staged inertial initialization (imu_init.py) once enough
        keyframes with attached preintegration segments exist. On success,
        assigns velocities to those keyframes, stores gravity/bias, and
        flips tracking.imu_initialized so future frames get IMU prediction
        and future keyframes get inertial BA.
        """
        kfs = [kf for kf in world_map.keyframes if kf.imu_preint is not None or kf.kf_seq == 0]
        kfs = sorted(kfs, key=lambda kf: kf.kf_seq)
        min_kf = self.cfg["imu"]["init_min_keyframes"]
        if len(kfs) < min_kf:
            return
        if kfs[-1].timestamp - kfs[0].timestamp < self.cfg["imu"]["init_window_seconds"]:
            return

        self.stats['imu_init_attempts'] += 1
        sync_samples = np.asarray(self.imu_raw_buffer) if self.imu_raw_buffer else np.zeros((0, 7))
        result = imu_init.initialize(
            kfs, sync_samples,
            gravity_mag=self.cfg["imu"]["gravity_magnitude"],
            min_gyro_std=self.cfg["imu"]["observability_min_gyro_std"],
            min_accel_std=self.cfg["imu"]["observability_min_accel_std"])

        if not result["success"]:
            self._log(f"[imu-init] not ready yet: {result['reason']}")
            return

        for kf, v in zip(kfs, result["velocities"]):
            kf.velocity = v
        self.bias_gyro = result["bias_gyro"]
        self.bias_accel = result["bias_accel"]
        self.tracking.gravity = result["gravity"]
        self.tracking.imu_initialized = True
        self._log(f"[imu-init] SUCCESS: gravity={np.round(result['gravity'], 3)} "
                  f"|g|={np.linalg.norm(result['gravity']):.3f} "
                  f"bias_gyro={np.round(result['bias_gyro'], 4)} "
                  f"bias_accel={np.round(result['bias_accel'], 4)}")

        # Fold the initialization straight into the map once (stands in for
        # ORB-SLAM3's VIBA1/VIBA2 re-refinement passes -- see
        # imu_init.py's docstring for why those aren't implemented here).
        local_inertial_bundle_adjust(world_map, self.camera, gravity=self.tracking.gravity,
                                     bias_gyro=self.bias_gyro, bias_accel=self.bias_accel,
                                     window=len(kfs), verbose=self.verbose)

    def _log(self, msg):
        if self.verbose:
            print(msg)

    # ── outputs ──────────────────────────────────────────────────────────

    def save_trajectory_plot(self, path="trajectory_python.png"):
        poses = [f.pose for f in self.frames if f.pose is not None]
        if len(poses) < 2:
            print("Not enough poses to plot.")
            return None

        c = np.array([p[:3, 3] for p in poses])
        kf_c = np.array([kf.camera_center() for m in self.atlas.maps
                            for kf in m.keyframes if kf.pose is not None])

        fig, axes = plt.subplots(1, 2, figsize=(14, 6))
        for ax, (i, j), (xl, yl), title in (
                (axes[0], (0, 2), ("X (m)", "Z (m)"), "Bird's-eye (X-Z)"),
                (axes[1], (0, 1), ("X (m)", "Y (m)"), "Front (X-Y)")):
            ax.plot(c[:, i], c[:, j], '-', lw=1.2, color='tab:orange', label='trajectory')
            if len(kf_c):
                ax.scatter(kf_c[:, i], kf_c[:, j], s=14, color='tab:blue',
                           zorder=4, label='keyframes')
            ax.scatter([c[0, i]], [c[0, j]], s=70, c='green', marker='o',
                       edgecolors='k', zorder=5, label='start')
            ax.scatter([c[-1, i]], [c[-1, j]], s=70, c='red', marker='s',
                       edgecolors='k', zorder=5, label='end')
            ax.set_xlabel(xl); ax.set_ylabel(yl); ax.set_title(title)
            ax.set_aspect('equal', adjustable='datalim')
            ax.grid(alpha=0.3); ax.legend(fontsize=8)

        fig.suptitle("Python ORB-SLAM3 reimplementation")
        fig.tight_layout()
        fig.savefig(path, dpi=130, bbox_inches='tight')
        plt.close(fig)
        print(f"Saved {path}")
        return path

    def save_map_ply(self, path="map_points.ply"):
        pts = [mp.position for m in self.atlas.maps
               for mp in m.good_map_points()]
        if not pts:
            return None
        pts = np.asarray(pts)
        with open(path, 'w') as f:
            f.write("ply\nformat ascii 1.0\n")
            f.write(f"element vertex {len(pts)}\n")
            f.write("property float x\nproperty float y\nproperty float z\n")
            f.write("end_header\n")
            for p in pts:
                f.write(f"{p[0]:.6f} {p[1]:.6f} {p[2]:.6f}\n")
        print(f"Saved {path} ({len(pts)} points)")
        return path

    def print_summary(self):
        n_posed = sum(1 for f in self.frames if f.pose is not None)
        total = len(self.frames)
        print("\n" + "=" * 60)
        print("  SUMMARY")
        print("=" * 60)
        print(f"Frames processed:    {total}")
        print(f"Frames with pose:    {n_posed} ({100.0 * n_posed / max(1, total):.1f}%)")
        print(f"Tracking successes:  {self.stats['tracked']}")
        print(f"Tracking losses:     {self.stats['lost']}")
        print(f"Keyframes:           {self.stats['keyframes']}")
        print(f"Keyframes culled:    {self.stats['keyframes_culled']}")
        print(f"Map points created:  {self.stats['points_created']}")
        print(f"Map points culled:   {self.stats['points_culled']}")
        print(f"Map points fused:    {self.stats['points_fused']}")
        print(f"Loop closures found: {self.stats['loops']}")
        print()
        print(self.atlas.summary())

        poses = [f.pose for f in self.frames if f.pose is not None]
        if len(poses) >= 2:
            c = np.array([p[:3, 3] for p in poses])
            path_len = float(np.sum(np.linalg.norm(np.diff(c, axis=0), axis=1)))
            net = float(np.linalg.norm(c[-1] - c[0]))
            print(f"\nPath length:      {path_len:.3f}"
                  f"{' m' if self.use_depth else ' (arbitrary units)'}")
            print(f"Net displacement: {net:.3f}"
                  f"{' m' if self.use_depth else ' (arbitrary units)'}")
            if not self.use_depth:
                print("  NOTE: monocular = no absolute scale. Distances are "
                      "relative only.")
        print("=" * 60)


# ── runners ──────────────────────────────────────────────────────────────

def run_realsense(args):
    """
    Live capture. Default path tracks on the COLOR image (fixed: intrinsics
    now genuinely come from the color stream that gets aligned-to and fed
    to the extractor, and baseline comes from real extrinsics -- see
    camera.py's docstring for what was wrong before).

    --ir switches to the recommended pipeline: track on the left infrared
    image instead. Global-shutter IR pairs better with future IMU
    preintegration than the rolling-shutter RGB sensor, and depth is
    natively registered to it (no rs.align needed at all). Only worth it if
    the facility has enough natural texture for emitter-off IR frames to be
    usable -- see the architecture notes. Uses emitter_on_off alternating
    mode: even frames keep the projector pattern (for depth), odd frames
    are clean for ORB (real-time isn't a priority, so halving the rate is fine).

    --imu (PHASE 8): enables the D435i's accel (250Hz) + gyro (200Hz)
    motion streams and feeds synchronized samples into slam.process() every
    iteration. Uses the exact same technique record.py's proven offline
    recorder already uses (motion frames arrive interleaved in the same
    `pipeline.wait_for_frames()` frameset as video/depth when enabled on
    one pipeline -- iterate the frameset and pick out `is_motion_frame()`
    entries, no separate thread or callback needed) -- see record.py's
    docstring for why this works and PROGRESS.md's Phase 8 section for
    why this was never actually wired here before, despite the tracking/
    initialization code that CONSUMES this data having existed since
    before Phase 6.
    """
    import pyrealsense2 as rs

    if args.ir:
        camera = Camera.from_realsense_ir(args.width, args.height, args.fps)
    else:
        camera = Camera.from_realsense(args.width, args.height, args.fps)
    print(camera)
    os.makedirs("calibration", exist_ok=True)
    camera.to_json("calibration/realsense_d435.json")

    slam = SLAMSystem(camera, use_depth=not args.mono, verbose=not args.quiet,
                      use_imu=args.imu)

    pipeline = rs.pipeline()
    config = rs.config()
    if args.ir:
        config.enable_stream(rs.stream.infrared, 1, args.width, args.height, rs.format.y8, args.fps)
        config.enable_stream(rs.stream.depth, args.width, args.height, rs.format.z16, args.fps)
    else:
        config.enable_stream(rs.stream.color, args.width, args.height, rs.format.bgr8, args.fps)
        config.enable_stream(rs.stream.depth, args.width, args.height, rs.format.z16, args.fps)
        align = rs.align(rs.stream.color)

    if args.imu:
        # PHASE 8: native rate (0 = sensor's own rate -- 250Hz accel /
        # 200Hz gyro on the D435i's BMI085, matching record.py exactly).
        config.enable_stream(rs.stream.accel, rs.format.motion_xyz32f, 0)
        config.enable_stream(rs.stream.gyro, rs.format.motion_xyz32f, 0)

    profile = pipeline.start(config)

    # PHASE 8: put every sensor's timestamps on ONE clock -- critical for
    # IMU (video and motion frames come from physically different sensors
    # with independent internal clocks otherwise). record.py has done this
    # unconditionally for a while; this path only ever did it inside the
    # --ir branch, which meant a plain RGB + --imu run never got it at
    # all. Doing it unconditionally, same as record.py.
    for sensor in profile.get_device().sensors:
        if sensor.supports(rs.option.global_time_enabled):
            sensor.set_option(rs.option.global_time_enabled, 1)

    if args.ir:
        depth_sensor = profile.get_device().first_depth_sensor()
        if depth_sensor.supports(rs.option.emitter_on_off):
            depth_sensor.set_option(rs.option.emitter_on_off, 1)
            depth_sensor.set_option(rs.option.emitter_enabled, 1)

    if args.imu:
        R_cam_imu = imu.load_T_cam_imu(args.t_cam_imu) if args.t_cam_imu else np.eye(3)
        if args.t_cam_imu is None:
            print("[imu] no --t_cam_imu given -- using identity rotation "
                 "(fine for a quick test; for real use, record a "
                 "T_cam_imu.txt via record.py --imu and pass it here)")

    total_target_frames = args.fps * args.seconds
    print(f"\nStreaming at {args.fps} FPS. Capturing approximately {total_target_frames} target frames ({args.fps} per second over {args.seconds} seconds).\n")

    t0 = time.time()
    processed_count = 0
    frame_counter = 0
    pending_ir_clean = None   # holds the last emitter-off IR frame while we wait for the paired depth
    last_process_t = None     # PHASE 8: wall-clock cursor for slicing IMU samples per process() call

    try:
        while processed_count < total_target_frames:
            frames = pipeline.wait_for_frames()

            # PHASE 8: pull out any motion samples riding along in this
            # frameset BEFORE the video-frame branching below (which uses
            # `continue` in several places -- IMU samples must not be
            # dropped just because this particular frameset didn't yield
            # a usable video frame this iteration).
            if args.imu:
                for f in frames:
                    if f.is_motion_frame():
                        mf = f.as_motion_frame()
                        md = mf.get_motion_data()
                        ts = mf.get_timestamp() / 1000.0   # ms -> s, global clock
                        stream = "accel" if mf.get_profile().stream_type() == rs.stream.accel else "gyro"
                        raw = np.array([ts, md.x, md.y, md.z])
                        (slam._live_accel_buf if stream == "accel"
                         else slam._live_gyro_buf).append(raw)

            if args.ir:
                ir = frames.get_infrared_frame(1)
                depth = frames.get_depth_frame()
                if not ir:
                    continue
                try:
                    emitter_on = bool(ir.get_frame_metadata(
                        rs.frame_metadata_value.frame_laser_power_mode))
                except Exception:
                    emitter_on = (frame_counter % 2 == 0)

                if emitter_on:
                    if pending_ir_clean is not None and depth:
                        color_img = pending_ir_clean
                        depth_img = np.asanyarray(depth.get_data()) if not args.mono else None
                        pending_ir_clean = None
                    else:
                        frame_counter += 1
                        continue
                else:
                    pending_ir_clean = np.asanyarray(ir.get_data())
                    frame_counter += 1
                    continue
            else:
                frames = align.process(frames)
                color = frames.get_color_frame()
                depth = frames.get_depth_frame()
                if not color:
                    continue
                frame_counter += 1
                color_img = np.asanyarray(color.get_data())
                depth_img = np.asanyarray(depth.get_data()) if (depth and not args.mono) else None

            processed_count += 1
            print(f"Processing target frame {processed_count}/{total_target_frames}")
            this_t = time.time() - t0

            imu_samples = None
            if args.imu:
                # PHASE 8: synchronize (interpolate accel onto gyro
                # timestamps, rotate into camera frame -- imu.py's
                # synchronize()) whatever's accumulated since the last
                # process() call, then slice to exactly this interval.
                # Buffers keep everything seen so far rather than being
                # cleared every iteration -- interpolation needs a little
                # context on both sides of the window to avoid edge
                # artifacts, and imu.slice_between already does the exact
                # windowing we need on the synchronized result.
                sync = imu.synchronize(
                    {"accel": np.asarray(slam._live_accel_buf) if slam._live_accel_buf else np.zeros((0, 4)),
                     "gyro": np.asarray(slam._live_gyro_buf) if slam._live_gyro_buf else np.zeros((0, 4))},
                    R_cam_imu=R_cam_imu)
                lo = last_process_t if last_process_t is not None else this_t - (1.0 / args.fps)
                imu_samples = imu.slice_between(sync, lo, this_t)
                # Trim buffers so they don't grow for the whole session --
                # keep a small tail before `lo` for the next interpolation.
                keep_from = lo - 0.05
                slam._live_accel_buf = [r for r in slam._live_accel_buf if r[0] >= keep_from]
                slam._live_gyro_buf = [r for r in slam._live_gyro_buf if r[0] >= keep_from]
                last_process_t = this_t

            slam.process(color_img, this_t, depth_image=depth_img, imu_samples=imu_samples)

    except KeyboardInterrupt:
        print("\nStopped by user.")
    finally:
        pipeline.stop()

    return slam


def run_frames(args):
    if args.calib:
        camera = Camera.from_json(args.calib)
    else:
        print("ERROR: --frames needs --calib <calibration.json>")
        sys.exit(1)
    print(camera)

    paths = sorted(glob.glob(os.path.join(args.frames, "*.png")) +
                   glob.glob(os.path.join(args.frames, "*.jpg")))
    if args.max_frames:
        paths = paths[:args.max_frames]
    if not paths:
        print(f"No images found in {args.frames}")
        sys.exit(1)
    print(f"Found {len(paths)} frames\n")

    slam = SLAMSystem(camera, use_depth=False, verbose=not args.quiet)
    for i, p in enumerate(paths):
        img = cv2.imread(p, cv2.IMREAD_GRAYSCALE)
        if img is None:
            continue
        slam.process(img, i / float(args.fps))
    return slam


def main():
    ap = argparse.ArgumentParser(description="Python ORB-SLAM3 reimplementation")
    src = ap.add_mutually_exclusive_group(required=True)
    src.add_argument("--realsense", action="store_true", help="live D435 input")
    src.add_argument("--frames", type=str, help="directory of image frames")

    ap.add_argument("--calib", type=str, help="calibration JSON (for --frames)")
    ap.add_argument("--seconds", type=int, default=30, help="capture duration")
    ap.add_argument("--max_frames", type=int, default=None)
    ap.add_argument("--width", type=int, default=640)
    ap.add_argument("--height", type=int, default=480)
    ap.add_argument("--fps", type=int, default=30)
    ap.add_argument("--mono", action="store_true",
                    help="ignore depth even on RealSense (monocular mode)")
    ap.add_argument("--ir", action="store_true",
                    help="track on left IR (global shutter, natively depth-"
                         "registered) instead of RGB. See camera.py docstring. "
                         "Only worth it if the facility has enough natural "
                         "texture for emitter-off IR frames to be usable.")
    ap.add_argument("--imu", action="store_true",
                    help="PHASE 8: enable the D435i's accel+gyro motion "
                         "streams and feed them into the pipeline for "
                         "IMU-predicted pose (tracking robustness through "
                         "fast rotation / motion blur) and IMU-coasted "
                         "recovery during brief tracking loss. --realsense "
                         "only -- --frames has no IMU source.")
    ap.add_argument("--t_cam_imu", type=str, default=None,
                    help="path to a T_cam_imu.txt (4x4) from record.py's "
                         "--imu extrinsics dump. Only the rotation block is "
                         "used (imu.py's lever-arm simplification). Falls "
                         "back to identity if omitted -- fine for a quick "
                         "test, but a real T_cam_imu measurement matters "
                         "for prediction accuracy.")
    ap.add_argument("--quiet", action="store_true")
    args = ap.parse_args()

    slam = run_realsense(args) if args.realsense else run_frames(args)

    slam.print_summary()
    slam.save_trajectory_plot()
    slam.save_map_ply()


if __name__ == "__main__":
    main()



"""
config.py — one YAML file for every tunable that used to be a hardcoded
default scattered across camera.py, tracking.py, local_mapping.py, and
bundle_adjust.py. Doesn't change any behavior by itself (defaults below
match what the code already used) -- it's here so that per-facility tuning
(texture, room scale, how aggressive to gate depth) doesn't require editing
source files, and so record.py / run_dataset.py can share one source of
truth for capture and replay settings.

Usage:
    from config import load_config
    cfg = load_config("config.yaml")   # or load_config() for defaults
    extractor = Extractor(**cfg["extractor"])
    tracking = Tracking(camera, extractor, matcher, world_map, **cfg["tracking"])
    local_mapping = LocalMapping(camera, matcher, world_map, **cfg["local_mapping"])
"""

import copy
import os
import yaml

DEFAULTS = {
    "capture": {
        "width": 640,
        "height": 480,
        "fps": 30,
        "use_ir": False,          # track on left IR instead of RGB -- see camera.py
        "use_depth": True,
        "enable_imu": False,      # set True once imu.py lands (Phase 3)
        "accel_rate_hz": 250,
        "gyro_rate_hz": 200,
    },
    "initializer": {
        "rgbd_min_points": 100,
        "mono_min_matches": 100,
        "mono_min_parallax_px": 2.0,
    },
    "extractor": {
        "nfeatures": 1200,
        "scale_factor": 1.2,
        "nlevels": 8,
        "ini_th_fast": 20,
        "min_th_fast": 7,
    },
    "tracking": {
        "min_matches_for_pose": 15,
        "keyframe_min_matches": 50,
        # PHASE 7: raised from 0.10 to 0.25 -- 0.10m keyframe spacing was
        # tuned for a small handheld test volume; at facility scale it
        # produces far more keyframes than needed and pushes every
        # downstream optimizer (BA, pose graph) into a larger problem
        # than the trajectory actually requires. See
        # PHASE7_ARCHITECTURE.md's config table.
        "keyframe_min_displacement": 0.25,
        "keyframe_max_frames": 20,
        # PHASE 7 (new): rotation-triggered keyframe insertion -- see
        # tracking.py's needs_new_keyframe. A pure rotation with
        # near-zero translation used to trigger no keyframe at all.
        "keyframe_min_rotation_deg": 15.0,
        # PHASE 7: Phase 4's dense RGB-D alignment fallback (use_dense_
        # fallback / dense_depth_trunc) was REMOVED this phase -- it
        # never worked reliably on real hardware, added an Open3D
        # dependency, and its purpose is now substantially addressed at
        # the front-end level by far-point triangulation and the
        # rotation keyframe trigger above. See tracking.py's Phase 7
        # comment in track() for the full account and the documented
        # fallback plan if a genuinely featureless area is still a
        # problem after these land.
    },
    "local_mapping": {
        "min_parallax_px": 2.0,
        "max_neighbors": 5,
        "culling_found_ratio": 0.25,
        "culling_min_obs": 3,
        # PHASE 7: raised from 100 -- real-time budget was never the
        # constraint here (offline/CPU-bound extraction dominates), and
        # the old cap combined with pure closest-first selection (see
        # below) meant coverage was both small AND clustered.
        "max_new_points_per_kf": 300,
        "depth_min": 0.3,
        # PHASE 7: raised from 3.5 to 6.0. The D435i's active depth
        # degrades past ~3-4m, which is exactly why points beyond
        # th_depth are now treated as FAR (triangulated, weakly trusted
        # for scale) rather than bare trusted depth -- see th_depth
        # below. Capping the overall sane range higher just means more
        # candidate far points are available to triangulate, not that
        # more bare-depth points are trusted.
        "depth_max": 6.0,
        # PHASE 7 (new): ORB-SLAM2's close/far split -- ~40x the D435i's
        # ~5cm IR baseline. See local_mapping.py's __init__ docstring
        # and PHASE7_ARCHITECTURE.md finding 3.3.
        "th_depth": 2.0,
        "depth_patch_radius": 2,
        "depth_rel_std_max": 0.02,
        # PHASE 7 (new): stratified point selection grid -- see
        # local_mapping.py's _stratified_select.
        "strat_grid_rows": 4,
        "strat_grid_cols": 4,
    },
    "bundle_adjust": {
        # BUGFIX (found during Phase 3 verification): this defaulted to
        # "scipy" through Phase 2. That was fine when it was still true
        # that most BA calls stayed small -- but Phase 3's extractor
        # rewrite gives every keyframe a genuinely richer, better-
        # distributed set of valid points/observations (that's the whole
        # point of the rewrite), which is enough on its own to push even
        # NORMAL, properly-culled BA windows (21-22 keyframes, nothing
        # pathological) into scipy's slow-scaling regime: measured at
        # ~6.6s PER CALL, 125.9 of 133.5s total in one 30-frame profile.
        # GTSAM completed the identical scenario in 5.74s total. Phase 2
        # built exactly the fix this needs; it just wasn't the default.
        #
        # PHASE 9: "isam2" is now the default backend for the live,
        # visual-only per-keyframe path -- one continuous incremental
        # GTSAM iSAM2 graph replacing the windowed local_bundle_
        # adjust_gtsam() call below entirely (see isam2_backend.py and
        # PHASE7_ARCHITECTURE_V2_REALTIME.md sec 2). "gtsam" and "scipy"
        # remain selectable for comparison/fallback -- window/max_iter
        # below only apply to those two, not to "isam2" (see the "isam2"
        # section below for its own tuning knobs). While IMU is
        # initialized on a run, this setting is ignored and Phase 8's
        # local_inertial_bundle_adjust handles optimization instead
        # regardless -- see run_slam.py's per-keyframe block for the
        # exact rule.
        "backend": "isam2",  # "isam2" (default, real-time), "gtsam", or "scipy"
        "window": 8,
        "max_iter": 15,
        "min_obs_to_optimize": 2,
        "huber_f_scale": 2.0,
        "virtual_baseline": 0.05,
    },
    "isam2": {
        # PHASE 9 (new): tuning for isam2_backend.py's ISAM2 instance.
        # Defaults are GTSAM's own recommended starting points, per
        # PHASE7_ARCHITECTURE_V2_REALTIME.md sec 2.4's "measure before
        # tuning" plan -- only change these if a real bring-up benchmark
        # (test_phase9_gate.py's timing test, then real hardware) shows
        # a specific need.
        "relinearize_threshold": 0.1,
        "relinearize_skip": 1,
        "anchor_sigma_pos": 1e-3,   # gauge-fixing prior on the first keyframe
        "anchor_sigma_rot": 1e-2,
        "odom_sigma_pos": 0.05,     # consecutive-keyframe odometry factor --
        "odom_sigma_rot": 0.05,     # see isam2_backend.py's "on the odometry
                                    # factor" docstring section for why this
                                    # factor exists at all
    },
    "loop_closing": {
        "min_keyframe_gap": 30,
        "vocab_words": 10000,      # Phase 5: NO LONGER USED -- ORBVocabulary
                                   # loads a fixed, real ~971,814-word
                                   # vocabulary from disk (see
                                   # orb_vocabulary.py); this key is kept
                                   # only so old configs don't error on
                                   # load, not read by anything anymore.
        "consistency_checks": 3,   # require same candidate over N consecutive KFs
        # PHASE 9: was hardcoded `n_kf % 10 == 0` in run_slam.py -- now
        # configurable, defaulting to 1 (every keyframe). Real-time
        # justification, not just "why not": a slow-moving robot's
        # keyframes are seconds apart, so the Atlas-wide BoW search this
        # gates is affordable every time, not just every 10th -- see
        # PHASE7_ARCHITECTURE_V2_REALTIME.md sec 3/7.
        "check_every_n_kf": 1,
    },
    "relocalization": {
        "min_inliers": 20,
        "top_n_candidates": 5,
    },
    "imu": {
        # BMI085 datasheet fallbacks -- measured (record.py's static-camera
        # calibration recording) is better, but these work to get running.
        "noise_gyro": 1.7e-2,      # rad/s / sqrt(Hz)
        "noise_accel": 2.0e-2,     # m/s^2 / sqrt(Hz)
        "gravity_magnitude": 9.81,
        "init_min_keyframes": 6,
        "init_window_seconds": 2.0,
        "observability_min_gyro_std": 0.05,
        "observability_min_accel_std": 0.3,
        "recently_lost_max_seconds": 2.0,   # IMU-only survival window (Phase 4)
        "ba_window": 8,
    },
    "dataset": {
        "images_dirname": "images",
        "depth_dirname": "depth",
        "imu_filename": "imu.csv",
        "manifest_filename": "manifest.csv",
    },
}


def load_config(path=None):
    """
    Returns a deep-merged dict: DEFAULTS overridden by whatever's in the
    YAML file at `path` (only the keys present in the file are overridden;
    everything else keeps its default). Returns DEFAULTS untouched if path
    is None or doesn't exist.
    """
    cfg = copy.deepcopy(DEFAULTS)
    if path and os.path.exists(path):
        with open(path, "r") as f:
            user_cfg = yaml.safe_load(f) or {}
        for section, values in user_cfg.items():
            if section in cfg and isinstance(values, dict):
                cfg[section].update(values)
            else:
                cfg[section] = values
    return cfg


def save_default_config(path="config.yaml"):
    """Write DEFAULTS to disk as a starting point for editing."""
    with open(path, "w") as f:
        yaml.safe_dump(DEFAULTS, f, sort_keys=False, default_flow_style=False)
    return path


if __name__ == "__main__":
    import sys
    out = sys.argv[1] if len(sys.argv) > 1 else "config.yaml"
    save_default_config(out)
    print(f"Wrote default config to {out}")












